# Serial Monitor V2 — 开发日志

> 按时间倒序记录每场对话的完成内容。日常查看进度 → 读 `当前状态.md`；追溯历史 → 读本文。

---

### ✅ 第九场（2026-06-15）— GitHub 公开发布

- ✅ 仓库根目录 `README.md`：logo 居中 + 中英双语切换 + 暗/亮预览截图 + 功能介绍 + 安装说明 + 协议格式 + FAQ + Roadmap
- ✅ `docs/README_en.md` 英文版（完整翻译），语言栏 `简体中文 · English` 可切换
- ✅ `assets/`：dark.png / light.png（预览截图）+ logo.png（终端串口图标）
- ✅ GitHub Release v2.1.0：上传安装包 + Release 正文含下载表格
- ✅ 仓库从 `D:\STM32CubeIDE_1.17.0\serial` 搬到 `E:\serial`
- ✅ 文档全面审计修复（7 个文件 29 处修正）
- ✅ 第二轮评估 92 分，评估标准新增 4 条方法论原则

### ✅ 第八场（2026-06-14）— 按键调色盘 + 评估改进

| # | 项目 | 说明 |
|:--:|------|------|
| 1 | 按键调色盘 | 40 色 Popup（Material Design 色板）+ hex 手动输入 + 实时预览，按键/滑杆共用 |
| 2 | 色块选中态 | 预设色块 + Popup 内部色块蓝色边框标记当前选中，切选中项自动跟随 |
| 3 | Popup 点击外部关闭 | `StaysOpen = false` |
| 4 | 按键闪烁跟色 | `FlashButtonBg` 不再硬编码蓝——自定义色按键闪自身颜色 |
| 5 | 按下/选中/Shift 边框跟色 | `CreateKeyButton` 三处硬编码蓝改为用自身颜色 |
| 6 | BytesToHex StringBuilder | `hex +=` 循环 → `sb.Append`，高频数据 GC 归零 |
| 7 | 未知协议日志 | `未知协议类型: [xxx]`，STM32 调协议不再静默 |
| 8 | HUD 颜色 DynamicResource | 3 处硬编码色 + 底色/边框 → `FindResource` / `DynamicResource`，切主题跟随 |

### ✅ 第七场（2026-06-14）— 滑杆+摇杆图片素材系统

| # | 项目 | 说明 |
|:--:|------|------|
| 1 | 关于页增强 | 运行时版本 + 架构 + Issue 反馈入口 |
| 2 | 快捷键提示页 | 6 条快捷键，键帽设计，分组，切主题适配 |
| 3 | 使用示例页 | 6 张卡片含协议格式+参数表+设备端代码+📋复制 |
| 4 | 异常保护 | 三条致命路径（Read/Write/路由/SendRaw）全部 try-catch |
| 5 | 串口配置 Tooltip | 8 个控件详细说明 |
| 6 | EXE 压缩 | 157→142MB（SatelliteResourceLanguages + DebugType + SystemResourceKeys） |
| 7 | ProtocolParser 单元测试 | 15 条，26ms 全部通过 |
| 8 | 接收区右键菜单 | 📋复制 / 📄全选 / 🗑清空 / ⏸暂停，ControlTemplate 蓝色高亮 |
| 9 | 快捷键提示文本 | 发送区底部更新 |
| 10 | 评估文档 | `评估标准-V2.md` + `评估报告-V2.md`，88.35 分 |

### ⚠️ 2026-06-14 — 合并回退修复（12 项）

> commit `3c7bd8e` 将按键面板冲回 Phase 4 早期版本。滑杆/摇杆/OLED 面板因代码在独立文件中未受影响。

| # | 问题 | 修复 |
|:--:|------|------|
| 1 | 按键重启后消失 | `IList` 兼容 `List<object>` 和 `ArrayList` |
| 2 | "清空全部"/"删除模块"弹窗 | 二次点击确认（3s 自动取消） |
| 3 | 侧面板路由：点按键不切回设置面板 | 新增 `SwitchSidePanelToKeys()` |
| 4 | 键盘布局下拉菜单样式缺失 | 套用 `ContextMenuMenuItemStyle` + 图标 |
| 5 | 键盘布局预设缺失（仅 3 种） | 新增 WASD+QE、F1-F12、逻辑按键对 |
| 6 | 按键颜色不可读（黄底白字） | 亮度公式判断前景色 |
| 7 | 编辑选中态盖掉自定义颜色 | 自定义色仅加蓝边框，不覆盖背景 |
| 8 | 按键反馈极弱/无感 | 自定义 KeyButtonTemplate + 缩放脉冲 + 背景闪烁 |
| 9 | 色块点击不即时刷新 | `PreviewMouseLeftButtonDown` + `e.Handled` |
| 10 | 键盘布局发送值小写 | `MakeKey` 空值默认用名称 + `FromDict` 迁移旧值 |
| 11 | 滑杆进度条缺失 | 自定义 `ColoredSliderStyle` |
| 12 | 滑杆/摇杆按住无 Q 弹感 | `SpringPress`（88%）+ `SpringRelease`（弹性回弹） |

涉及文件：`KeyViewModel.cs`, `KeyPanelViewModel.cs`, `Panels/Keys.cs`, `Panels/Sliders.cs`, `Panels/Joystick.cs`, `Views/Animation.cs`, `MainWindow.xaml`, `Serial Monitor.exe`

### ✅ 第六场（2026-06-14）— 性能优化 + 构建修复

**绘图滚动流畅度：**
- 删除 `ApplyXAxisWindow()` 和 `RecalcYAxis()` 冗余 `InvalidatePlot` → 每帧 2 次→1 次
- `RecalcYAxis()` 只扫可见窗口 O(5000)→O(200)

**滑杆拖拽流畅度：**
- 新增 `_sliderPartsCache` 字典，`Loaded` 时缓存模板部件，`ValueChanged` 零树遍历

**构建修复（7 小时 icon.ico 崩溃）：**
- 根因：`icon.ico` 移到 `Icons/` 后 `MainWindow.xaml` `Icon="icon.ico"` 路径没改
- 崩溃日志增强：静态构造器注册 handler + 递归输出 InnerException 链
- `global.json` 锁定 SDK 8.0.422

### ✅ 第五场（2026-06-14）— 文件结构整理

- 10 个 partial class .cs 移入 `Panels/` + `Views/` + `App/`
- `icon.ico` 移入 `Icons/`，csproj 改名 `SerialMonitor.csproj`
- 根目录从 22+ 条目精简到 6 文件 + 7 目录

### ✅ 第四场（2026-06-14）— 剪贴板修复 + 波形暂停恢复 + 摇杆三风格

- 剪贴板 `SafeSetClipboard`：后台 STA 线程
- 波形暂停恢复：`ApplyXAxisWindow` 时间 clamp 3s-60s
- ContextMenu 暗色：加 `PlacementTarget`（约束 #24）
- 摇杆三风格：手柄风 / 极简风 / 经典风，偏好持久化

### ✅ 第三场（2026-06-14）— 曲线详情交互打磨 + 信号分析修正

- 单击曲线 / 📊详细按钮双入口 → 跨标签页跳转
- ←返回按钮固定顶部
- 三个复制按钮竖排 + 抖动动画加速
- 正弦波误判方波修复：CrestFactor + 边沿陡度三因子
- 追踪框暗色：`TextElement.SetForeground(plotView, Brushes.Black)`

### ✅ Phase 6 — .NET 8 升级（2026-06-13）

- .NET Framework 4.8 → .NET 8，csproj SDK 风格
- OxyPlot 2.1.0（DLL 手动）→ 2.2.0（NuGet），AvalonEdit → NuGet 6.3.1
- `PreferenceService`：`JavaScriptSerializer` → `System.Text.Json`
- 框架依赖单文件发布 1.95MB

### ✅ Phase 4 — 四控制面板（2026-06-13）

| 面板 | 协议 | 文件 |
|------|------|------|
| 按键 | `[key,name,state]` | `KeyViewModel.cs`, `KeyPanelViewModel.cs`, `Panels/Keys.cs` |
| 滑杆 | `[slider,name,val]` | `SliderViewModel.cs`, `SliderPanelViewModel.cs`, `Panels/Sliders.cs` |
| 摇杆 | `[joystick,id,x1,y1,x2,y2]` | `JoystickViewModel.cs`, `JoystickPanelViewModel.cs`, `Panels/Joystick.cs` |
| OLED | `[display,x,y,text,size,#color]` | `DisplayPanelViewModel.cs`, `Panels/Display.cs` |

### ✅ Phase 3 — OxyPlot 波形面板（2026-06-12）

- PlotViewModel + HUD + 滚动/扫描双模式 + 30Hz 限流 + CSV 导出

### ✅ Phase 2 — VS Code 三栏布局（2026-06-12）

- 竖排图标栏 + 左侧面板 220px + 6 标签 RadioButton `GroupName="IconBar"`

### ✅ Phase 1 — MVVM 架构（2026-06-12）

- DataConverter / ProtocolParser / SerialPortSession / PreferenceService 提取
