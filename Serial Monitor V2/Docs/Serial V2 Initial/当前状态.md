# Serial Monitor V2 — 当前状态（初始开发完成版）

> ⚠️ **历史存档**。本文是 V2 初始开发（Phase 1-4 + Phase 6）完成时的完整记录。
> 后续改动见上层 `Docs/开发日志.md`，修缮阶段计划见 `../Serial V2 修缮/开发计划2.md`。

---

> **软件名称**：Serial Monitor V2 v2.1.0
> **上一稳定版**：Serial Monitor v1.0.0（位于 `../串口助手-WPF-v1/`，只读不动）
> **目标框架**：.NET 8 + CommunityToolkit.Mvvm + OxyPlot 2.2.0 + AvalonEdit 6.3.1
> **工作区**：本文件夹 `Serial Monitor V2/`
> **当前阶段**：✅ Phase 1-4 + Phase 6 完成 → 维护/随需改进
> **交付物**：`Serial Monitor.exe`（~1.95MB 框架依赖单文件，PublishSingleFile=true + SelfContained=false）
> **编译命令**：`dotnet publish -c Release`（需 .NET 8 SDK，global.json 已锁定 8.0.422）
> **测试命令**：`dotnet test SerialMonitor.Tests/SerialMonitor.Tests.csproj`（72 条，28ms 全部通过）
> **评估得分**：首轮 88.35 → 第二轮 92（优秀）

---

## 一、项目起源

### 1.1 一切的开始：改了三个默认值

用户使用 B 站 UP 主**江科大**的 WinForms 串口助手 V1.1（位于 `../串口助手/`），功能完善但默认参数对不上：

| 设置 | 江科大默认 | 用户习惯 |
|------|:---:|:---:|
| 波特率 | 9600 | **115200** |
| 收发模式 | HEX | **文本** |
| 编码 | GBK | **UTF-8** |

每次打开软件都要改这三个选项——于是有了第一个 commit：改默认值。**整个项目的精神基因就是"工具应该记住我怎么用"。**

### 1.2 为什么开 WPF 版

WinForms 功能够了但界面太丑——无圆角、无动画、无阴影、无暗色主题。WPF 是最佳折中：串口逻辑复用江科大 `System.IO.Ports`（成熟稳定），UI 完全重写。

53 个 commit、四轮完善后，v1.0.0 得分 84.5/100，30+ 功能稳定运行。

### 1.3 为什么开 V2

v1.0.0 是**纯数据收发工具**。用户想要的是**理解数据含义的工具**：

```
STM32 发送：  Serial_Printf(&huart1, "[plot,P,%f][plot,I,%f][slider,kp,%f]\r\n", p, i, kp);

PC 软件收到：
  接收区：    14:23:05.123 -> [plot,P,0.5][plot,I,0.3][slider,kp,17]    ← 原样显示
  波形图：    两条曲线 P / I 实时更新                                      ← 新增
  滑杆面板：  kp = 17%，可拖拽发回 STM32                                  ← 新增
```

同时借这个机会把代码架构从 partial class 升级为 MVVM。

---

## 二、v1.0.0 资产

### 2.1 已完成的 30+ 功能

| 分类 | 功能 |
|------|------|
| 串口核心 | 动态扫描串口、17 预设 + 自定义波特率、HEX/文本收发、GBK/UTF-8 编码、硬件流控（RTS/CTS + XON/XOFF）、DTR/RTS 控制信号、启动自动选串口、5 层异常分类 |
| 接收区 | AvalonEdit 虚拟化渲染、彩色日志（系统/发送回显/接收）、时间戳三格式、行号跟随主题、智能滚屏锁定（≤8px）、暂停显示+缓冲+满提示、Ctrl+F 搜索（关键字/正则/大小写敏感）、日志导出 |
| 发送区 | 快捷发送面板（chip 按钮+右键编辑删除+预置 AT 指令）、发送历史（最近 20 条去重）、HEX 实时格式化、定时发送、换行符可选（\r\n/\n/\r/无）、发送后自动清空、Enter=发送 / Shift+Enter=换行 |
| 暗色主题 | VS Code Dark+ 风格、21 色 DynamicResource 全量覆盖（96 处）、一键切换 |
| 状态栏 | 状态圆点 280ms 渐变动画、TX/RX 流量统计+右键重置+持久化、USB 热插拔检测（WM_DEVICECHANGE）+ 自动重连（500ms×10 次） |
| 动效 | 圆点 ColorAnimation（280ms）、按钮 Opacity 脉冲（130ms）、ToggleSwitch Canvas.Left 滑动（200ms）、系统消息淡入淡出（200ms） |
| 健壮性 | 崩溃日志（crash.log）、死锁修复（DataReceived 退订）、日志上限 2000 + 裁剪修正、偏好文件 try-catch |

### 2.2 代码文件（v1 原始 — 6 个 partial class）

| 文件 | 行数 | 职责 |
|------|:---:|------|
| `MainWindow.xaml` | 825 | 全量 UI + 21 色调色板 + 9 套 ControlTemplate |
| `MainWindow.xaml.cs` | ~1250 | 串口核心 + 事件处理 + 快捷键 + 流量统计 |
| `MainWindow.Log.cs` | ~205 | AvalonEdit 彩色日志 + LogColorizer + 搜索 + 暂停缓冲 |
| `MainWindow.QuickSend.cs` | 336 | 快捷发送按钮面板 + 发送历史 |
| `MainWindow.Settings.cs` | ~130 | 窗口记忆 + 偏好存取 |
| `MainWindow.Theme.cs` | 50 | 21 色暗/亮主题切换 |
| `MainWindow.Animation.cs` | 56 | 3 种动效辅助方法 |

---

## 三、V2 新增内容

### 3.1 协议格式

```
格式：  [类型, 参数1, 参数2, ...]
       方括号包裹一条消息。多括号可同行，普通文本可混排。

plot           [plot,曲线名,数值]             波形图数据，曲线名自动成为图例
plot-clear     [plot-clear]                   清空波形图
slider         [slider,名称,百分比]            滑杆控件
key            [key,名字,状态]                按键控件（状态: down/up）
joystick       [joystick,编号,h1,v1,h2,v2]    摇杆控件
display        [display,x,y,"文本",字号]      虚拟 OLED，文本含逗号须用引号包裹
display-clear  [display-clear]                清空虚拟 OLED
```

### 3.2 数据流

```
单向（STM32 → PC）：
  串口字节 → DataReceived → 文本行 →
    ├── ProtocolParser
    │   ├── [plot,...]  → PlotViewModel       （追加数据点）
    │   ├── [slider,...]→ SliderPanelViewModel（更新滑杆位置）
    │   ├── [key,...]   → KeyPanelViewModel   （更新按键状态）
    │   ├── [display,...]→ DisplayPanelViewModel（渲染到虚拟 OLED）
    │   └── 普通文本    → 接收区（原样显示）
    │
    └── 接收区：LogReceived(text) ← 跟 v1 一模一样

双向（PC → STM32）：
  控件面板操作 → 编码为 [type,...] → SerialPortSession.Send() → 串口发出
    ├── 拖滑杆 kp → "[slider,kp,17]\r\n"
    ├── 点按键 4  → "[key,4,down]\r\n"
    └── STM32 通过 UART 协议接收 → 拆包执行
```

### 3.3 文件结构

```
Serial Monitor V2/
├── App.xaml / MainWindow.xaml          ← WPF 入口（根目录）
├── SerialMonitor.csproj                ← .NET 8 SDK-style
├── global.json                         ← 锁定 SDK 8.0.422
├── App/                                ← XAML code-behind
│   └── App.xaml.cs / MainWindow.xaml.cs
├── Core/
│   ├── Services/                       ← DataConverter / ProtocolParser / SerialPortSession / PlotAnalyzer / PreferenceService
│   ├── Models/                         ← ProtocolMessage / KeyViewModel / SliderViewModel / JoystickViewModel
│   └── ViewModels/                     ← MainViewModel / PlotViewModel / KeyPanelViewModel / SliderPanelViewModel / JoystickPanelViewModel / DisplayPanelViewModel
├── Panels/                             ← Keys.cs / Sliders.cs / Joystick.cs / Display.cs / PlotDetail.cs
├── Views/                              ← Animation.cs / Log.cs / QuickSend.cs / Settings.cs / Theme.cs
├── Icons/                              ← icon.ico + tabs/ + joystick/ + sliders/
├── Docs/                               ← 文档
├── Properties/                         ← AssemblyInfo.cs
└── Serial Monitor.exe                  ← ~1.95MB 单文件交付
```

### 3.4 VS Code 风格三栏布局

```
┌──────────────────────────────────────────────────┐
│ 串口 [COM3▼] [115200▼] [打开]            v2.0.0 │ ← 精简顶栏
├──┬────────────┬──────────────────────────────────┤
│📡│ 侧面板     │                                  │
│  │ (220px)    │  主内容区                        │
│📈│ 内容随     │  (接收/绘图/按键/滑杆/OLED)      │
│  │ 图标切换   │                                  │
│🎮│            │                                  │
│  │            │                                  │
│⚙│            │                                  │
├──┴────────────┴──────────────────────────────────┤
│ ● 已连接 │ TX/RX                                │ ← 状态栏
└──────────────────────────────────────────────────┘
```

---

## 四、阶段完成记录

### Phase 1 — MVVM 架构（2026-06-12）

| # | 步骤 | 文件 | 
|:--:|------|------|
| 3 | DataConverter 提取 | `Core/Services/DataConverter.cs` |
| 4 | ProtocolParser | `Core/Services/ProtocolParser.cs` + `Core/Models/ProtocolMessage.cs` |
| 5 | SerialPortSession 提取 | `Core/Services/SerialPortSession.cs` |
| 6 | MainViewModel 骨架 | `Core/ViewModels/MainViewModel.cs` |
| 7 | 4 个面板 VM 骨架 | `PlotViewModel.cs` 等 |
| 8 | PreferenceService JSON 化 | `Core/Services/PreferenceService.cs`（prefs.json 替代 window.cfg） |
| 9 | MainWindow 瘦身 | 改为 _session 事件订阅 |
| 10 | 编译验证 | Release → 0 警告 0 错误 |

### Phase 2 — VS Code 三栏布局（2026-06-12）

- 图标栏：6 个 RadioButton，`GroupName="IconBar"` 跨容器强制互斥
- 侧面板折叠：`colSidePanel.Width` 控制，折叠 28px，展开 220px
- ⚠️ **Path.Stroke 绑定崩溃**：改用 Rectangle+OpacityMask+ImageBrush
- ⚠️ **IsChecked 在 XAML 中**：全部移到构造器 InitializeComponent() 之后

**Phase 2 修复的 10 个 Bug：**

| Bug | Fix |
|-----|------|
| 绘图→点设置，主区跳回收区 | `_previousContentTab` 变量 |
| 窗口太小+不记忆 | 默认 960×640 + `LoadOrMigrate` 修正 |
| 布局崩坏 | MinHeight 500→560 + padding 收紧 |
| 图标栏互斥失效 | `GroupName="IconBar"` 显式归组 |
| 折叠后找不到展开按钮 | 折叠按钮移顶部，宽度 28px |
| Path.Stroke 绑定崩溃 | Rectangle+OpacityMask+ImageBrush |
| 偏好不记忆 | `MigrateFromLegacy()` 预检查 |
| COM 口重复 | ScanPorts + `Distinct()` |
| 侧面板拖拽过小消失 | MinWidth 28→120 |
| 拖拽触发点与面板错位 | Margin 6→0 |

### Phase 3 — OxyPlot 波形面板（2026-06-12）

- PlotViewModel + HUD + 滚动/扫描双模式 + CSV 导出
- 30Hz 刷新限流（`_dirty` 标记 + 33ms 间隔）
- ⚠️ **OxyPlot 控件不放 XAML**：在 C# 中 `new`，约束 #19
- ⚠️ **PlotViewModel 在 LoadWindowSettings 前初始化**：约束 #21

**Phase 3 踩坑记录：**

| # | 坑 | 根因 | 方案 |
|:--:|-----|------|------|
| B1 | `oxy:PlotView` 在 XAML | BAML 编译器加载第三方程序集失败 | C# 中 `new` |
| B2 | `IsChecked="True"` 在 XAML | BAML 加载期触发事件 | 构造器中设 |
| B3 | `_plotVM` 初始化顺序 | `LoadWindowSettings` 触发主题 → null | 先初始化 |
| B4 | `Axis.Minimum/Maximum` 无效 | OxyPlot 2.x 需 `Axis.Zoom()` | 统一用 Zoom |
| B5 | `RecalcYAxis` 只扫 `_series` | 测试曲线直接加 `Model.Series` | 遍历 `Model.Series` |
| B6 | 每个数据点都 `InvalidatePlot` | 全量重绘 | 30Hz 限流 |

### Phase 4 — 四控制面板（2026-06-13）

| 面板 | 协议 | 新增文件 |
|------|------|---------|
| 按键 | `[key,name,state]` | `KeyViewModel.cs`, `KeyPanelViewModel.cs`, `Panels/Keys.cs` |
| 滑杆 | `[slider,name,val]` | `SliderViewModel.cs`, `SliderPanelViewModel.cs`, `Panels/Sliders.cs` |
| 摇杆 | `[joystick,id,x1,y1,x2,y2]` | `JoystickViewModel.cs`, `JoystickPanelViewModel.cs`, `Panels/Joystick.cs` |
| OLED | `[display,x,y,text,size,#color]` | `DisplayPanelViewModel.cs`(含 DisplayItem), `Panels/Display.cs` |

### Phase 6 — .NET 8 升级（2026-06-13）

- .NET Framework 4.8 → .NET 8，csproj 旧式 131 行 → SDK 风格 37 行
- OxyPlot 2.1.0（DLL 手动引用）→ 2.2.0（NuGet）
- AvalonEdit DLL → NuGet 6.3.1.120
- `PreferenceService.cs`：`JavaScriptSerializer` → `System.Text.Json`
- 框架依赖单文件发布：`Serial Monitor.exe`（~1.95MB）
- 版本号 1.2 → 2.0

**Phase 6 踩坑记录：**

| # | 坑 | 现象 | 根因 | 方案 |
|:--:|-----|------|------|------|
| B1 | 启动即崩溃 | `TypeConverterMarkupExtension` | XAML 声明了未使用的 `xmlns:oxy` | 删除未用 xmlns（约束 #22） |
| B2 | 滑杆拖拽卡住 | STM32 回显触发 `RefreshSlidersUI()` 重建 | 拖拽中控件被销毁 | 不调 Refresh，直接更新控件值 |
| B3 | 滑杆拖拽只移 1 单位 | .NET 8 下 `IsSnapToTickEnabled=true` 异常 | WPF 行为变化 | 去掉 IsSnapToTick，手动 `Math.Round` |

### 打磨阶段（2026-06-14 ~ 2026-06-15）

**第三场**：曲线详情交互 + 信号分析（CrestFactor + 边沿陡度防正弦误判方波）+ 追踪框暗色修复

**第四场**：剪贴板 STA 线程安全 + 波形暂停恢复 + 摇杆三风格（手柄/极简/经典）+ ContextMenu 暗色修复

**第五场**：文件结构整理（partial class 分 Panels/Views/App，icon.ico 移 Icons/，csproj 改名）

**第六场**：
- 🧨 **7 小时 icon.ico 崩溃**：移动文件后 XAML 路径没改 → BAML 缓存掩盖 → 重编崩溃 → 回退仍崩（回退的是 OLED 代码不是 icon 路径）→ 6 轮排查失败 → 最终根因：`Icon="icon.ico"` 没改。教训写入约束 #26-27。
- 绘图滚动优化：删冗余 `InvalidatePlot`，Y 轴 O(5000)→O(200)
- 滑杆拖拽零树遍历：`_sliderPartsCache`
- `BytesToHex` StringBuilder：GC 压力归零
- 崩溃日志增强：递归 InnerException 链

**第七场**：滑杆/摇杆图片素材系统 + 接收区右键菜单 + 异常保护全路径封堵 + 使用示例页 6 张卡片 + 快捷键提示页键帽设计

**第八场**：按键调色盘 40 色 Material Design Popup + hex 输入 + 按键颜色 100% 自主 + 未知协议日志 + HUD 颜色 DynamicResource

**第九场**：GitHub 公开发布 + README 双语 + Roadmap + 文档全面审计 + 第二轮评估 92 分

---

## ⚠️ 硬性约束（27 条，每次改动前必读）

| # | 规则 |
|:--:|------|
| **1** | **4 个数据转换方法禁止修改逻辑**——`BytesToHex`/`HexToBytes`/`BytesToText`/`TextToBytes` 算法一行不准改 |
| **2** | **串口底层操作保留原行为**——`serialPort.Read()`/`Write()`/`Open()`/`Close()` 调用方式不变 |
| **3** | **接收区显示行为完全不变**——`LogReceived`/`LogSent`/`LogSystem`/`AppendColoredLine` 原样 |
| **4** | **用户是新手小白**——编译成 .exe，用户只双击运行 |
| **5** | **Git 已上传云端**——不 force push，中文 commit message |
| **6** | **现有 XAML 设计系统是核心资产**——21 色 DynamicResource、9 套 ControlTemplate、圆角卡片 |
| **7** | **`{DynamicResource}` 必须用 Dynamic**——全 XAML 96 处，新增控件必须 Dynamic |
| **8** | **AvalonEdit 已 NuGet 管理**——编译自动包含，不需要手动复制 DLL |
| **9** | **工作区隔离**——`Serial Monitor V2/` 唯一工作区；`../串口助手-WPF-v1/` 只读 |
| **10** | **编译即交付**——`dotnet publish -c Release` → 单文件 exe 放项目根 |
| **11** | **新 .cs 文件自动包含**——SDK-style csproj 自动扫目录，但 Resource（PNG）需声明 |
| **12** | **v1 功能一个不能丢**——行号/系统日志/智能滚屏/搜索/HEX/定时发送/快捷发送/热插拔/流量统计/崩溃日志 |
| **13** | **Exception 保留原中文错误信息**——用户凭这些消息自己排查 |
| **14** | **🚫 Path.Stroke 禁止 {Binding}**——BAML 反序列化崩溃。用 `Rectangle+OpacityMask+ImageBrush` |
| **15** | **RadioButton 跨容器用 GroupName**——DockPanel+StackPanel 嵌套打破默认互斥 |
| **16** | **csproj 已是 SDK-style**——Phase 6 升级 .NET 8 时转的，不要转回旧格式 |
| **17** | **编译用 dotnet publish**——`PublishSingleFile=true, SelfContained=false`，global.json 锁定 8.0.422 |
| **18** | **侧面板折叠/拖拽共存**——MinWidth=120，折叠时临时降为 0 |
| **19** | **🚫 第三方 WPF 控件不放 XAML**——OxyPlot 等必须在 C# 中 `new`，XAML 只声明命名空间 |
| **20** | **初始选中状态不放 XAML**——`IsChecked`/`SelectedIndex` 在构造器 `InitializeComponent()` 之后设置 |
| **21** | **PlotViewModel 在 LoadWindowSettings 之前初始化**——否则暗色模式颜色失效 |
| **22** | **🚫 未使用的 3rd-party xmlns 禁止在 XAML**——声明了就崩，只声明实际使用的 |
| **23** | **🚫 整理 Git 历史必须先 `git commit` 保存当前工作区**——禁止把未提交修改混入 rebase |
| **24** | **🚫 Merge 后必须验证按键面板**——`Panels/Keys.cs`/`KeyPanelViewModel.cs`/`KeyViewModel.cs`/`Views/Animation.cs` |
| **25** | **🚫 代码创建的 ContextMenu/Popup 必须设 PlacementTarget**——否则暗色模式无效 |
| **26** | **🚫 禁止移动 XAML 文件到子目录**——BAML 缓存冲突导致启动崩溃 |
| **27** | **🚫 移动资源文件后必须 grep 所有引用**——XAML 相对路径编译器不验证，运行时才炸 |

## 讨论决策（38 条，不再争论）

| # | 决策 | 原因 |
|:--:|------|------|
| 1 | 不换框架，继续 WPF | 现有 53 commit 资产 |
| 2 | 升 .NET 8 ✅ | Phase 6 完成 |
| 3 | partial class → MVVM | 可测试、可扩展 |
| 4 | 协议格式 `[type,name,value]` | 数据自带语义 |
| 5 | 单向显示 + 双向回控 | 控件面板不只是看 |
| 6 | 接收区行为不变 | 协议解析是额外动作 |
| 7 | 先做波形图，控件面板预留 | 绘图是 P0 |
| 8 | 多串口非刚需 | 单串口+协议够用 |
| 9 | 绘图用 OxyPlot | .NET 最成熟免费库 |
| 10 | Skills 由 AI 判断 | 多数 Web 方向不适用 |
| 11 | Phase 2 先 UI 重组 | 不整 UI 就堆面板会挤 |
| 12 | 标签栏动态标签 | 用户添加控件后标签出现 |
| 13 | 侧面板内容随图标切换 | 可折叠+固定 |
| 14 | 设置是标签不是弹窗 | 图标栏最右 |
| 15 | 顶栏只放核心 | 串口+波特率+打开+版本 |
| 16 | 偏好升级为 JSON | prefs.json 替代 window.cfg |
| 17 | 控件统一编辑模式 | 工具栏 [编辑]/[完成] |
| 18 | 按键发送灵活化 | HEX/文本/固定数据包 |
| 19 | display 文本引号包裹 | 含逗号时 `"text,18"` |
| 20 | 顶部工具栏统一模式 | 主区顶部操作按钮 |
| 21 | 绘图数值为图上 HUD | 半透明覆盖，鼠标穿透 |
| 22 | 快捷键提示侧面板子页 | 不弹窗不打断 |
| 23 | 空状态设计 | 居中灰色提示 |
| 24 | 控件编辑选中高亮 | AccentBrush 2px 边框 |
| 25 | 暂停按钮 Toggle 态 | ⏸暂停 ↔ ▶继续 |
| 26 | 设置面板分组滚动 | 不用手风琴 |
| 27 | 侧面板固定 220px | 不做 GridSplitter |
| 28 | 按键三层大小方案 | 预设/手动/颜色 |
| 29 | Ctrl+多选批量改 | ListBox Extended |
| 30 | 关于页内容 | 制作人+不绑定MCU+路径变量 |
| 31 | 数据目录改名 | `串口助手WPF`→`SerialMonitor` |
| 32 | 标签手动切换 `[+]` Popup | 蓝色✓/灰色切换 |
| 33 | 设置面板导航菜单 | 二级子页 `←返回` |
| 34 | 二次点击抖动 | ScaleX 水平脉冲 |
| 35 | ComboBox 暗色修复 | 完整 ControlTemplate |
| 36 | 主题切换补齐 | 摇杆/OLED+SystemColors |
| 37 | Git LFS | exe 超 100MB 用 LFS |
| 38 | GitHub MIT License | `github.com/Encaron/SerialMonitor` |

---

## 🧨 标志性 Bug：7 小时 icon.ico 崩溃

OLED 背景色实验触发重编 → 启动崩溃 → 回退代码仍崩 → 6 轮错误假设 → 最终根因：`MainWindow.xaml` 的 `Icon="icon.ico"` 路径没跟着文件移动更新。旧 BAML 缓存掩盖了问题，重编后才暴露。

**教训**（约束 #26-27）：
1. 移动资源后必须 `grep -r "旧文件名"` 全局搜
2. 崩溃日志必须递归输出 InnerException 链
3. "回退后还崩"≠"回退不完整"——可能是回退的代码和 bug 不是同一个
4. XAML 相对路径编译器不验证——只到运行时才炸

---

## 给新 AI 的补充说明

### v1 代码里藏着的坑
- partial class 跨文件依赖——改之前先 grep
- AvalonEdit 不走 MVVM——保持 code-behind
- DataReceived 线程安全——所有 UI 操作 `Dispatcher.Invoke`
- 等宽字体回退链：`Sarasa Mono SC` → `Consolas` → `Courier New`
- 行缓冲 100ms + 智能滚屏 ≤8px
- 串口打开 5 层异常全部同步写系统日志

### 实施优先级直觉
- 先做无依赖的（DataConverter → ProtocolParser → SerialPortSession → VM 骨架）
- Phase 2 的第 1 步（重写 MainWindow.xaml 壳子）是整个项目最重要的单步
- Phase 3 先验证 OxyPlot 能渲染一根静态线，再堆 UI

### 什么不要做
- 不换框架
- 不 Canvas 自由定位
- 不面板拖拽调整宽度
- 不改 4 个数据转换算法
- 不用 `{StaticResource}`
- 不把暂停/清除/CSV 放进侧面板
- 不碰 `../串口助手-WPF-v1/`
- 不 force push

---

## 相关文档

| 文件 | 内容 |
|------|------|
| `开发计划.md` | 详细阶段计划 + 技术选型 + 实施细节 |
| `开发日志.md` | 按场次的完成记录（同目录） |
| `评估标准-V2.md` | 6 维评估方法论 |
| `评估报告-V2.md` | 首轮评估 88.35 |
| `评估报告-V2-第二轮.md` | 第二轮评估 92 分 |
| `MCU/按键测试协议.md` | STM32 端按键处理代码 |
| `MCU/OLED绘图协议.md` | 未来绘图指令 STM32 端封装 |
| `../串口助手-WPF-v1/当前状态.md` | v1.0.0 原始文档（只读） |
