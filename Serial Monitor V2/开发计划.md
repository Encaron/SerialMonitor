# Serial Monitor V2 — 开发计划

> **配套文档**：先读 `当前状态.md` 建立全局认知，再读本文件了解实施细节。

---

## 一、阶段概览

```
Phase 1 — 架构整理（铺路，不动 UI，行为与 v1.0.0 完全一致）
Phase 2 — UI 重组（标签栏 + 顶栏 + 设置弹窗，布局脱胎换骨）
Phase 3 — 波形图面板（第一个新功能）
Phase 4 — 控件面板（按键→滑杆→OLED→摇杆）
Phase 5 — 多串口（需要时）
Phase 6 — .NET 8 升级 + 打磨
```

每 Phase 编译产出可运行的 exe。

---

## Phase 1：架构整理

### 目标
代码从 partial class 升级为 MVVM。**功能行为与 v1.0.0 完全一致——用户看到的界面一模一样。**

### 实施顺序（先验证，再搬家）

```
1. 读 v1 代码 → 理解 6 个 partial class 的职责和依赖关系
2. NuGet 引入 CommunityToolkit.Mvvm
3. DataConverter.cs（纯静态、无依赖 → 马上可验证）
4. ProtocolParser.cs（无外部依赖，输入字符串输出 ParseResult）
5. SerialPortSession.cs（从 MainWindow 搬家，依赖 System.IO.Ports）
6. MainViewModel.cs（骨架，持有 SerialPortSession）
7. PlotViewModel / SliderPanelViewModel / KeyPanelViewModel / DisplayPanelViewModel
   （全骨架——空类，只放一个 Title 属性，为后续 Phase 占位）
8. PreferenceService.cs + prefs.json（替代 window.cfg）
9. MainWindow.xaml.cs 瘦身 + MainWindow.xaml 调整绑定
   （删掉搬走的方法，改为调用 Session/VM，不改 UI 布局）
10. 编译验证 → 行为与 v1.0.0 完全一致
```

**原则**：先建无依赖的（DataConverter、ProtocolParser），再建依赖少的（VM 骨架），最后动 MainWindow。每一步编译通过再走下一步。

### 1.1 提取 SerialPortSession

```
新文件：SerialPortSession.cs

从 MainWindow.xaml.cs 搬出：
  - serialPort / isSerialOpen
  - byteBuffer / receiveLineBuffer / flushTimer
  - OpenSerialPort() / CloseSerialPort()
  - serialPort_DataReceived()（字节读取 + Dispatcher.Invoke）
  - SendData() / SendRaw()
  - txByteCount / rxByteCount
  - DTR/RTS 控制
```

类签名：
```csharp
public class SerialPortSession : IDisposable
{
    public event Action<string> LineReceived;   // 完整行文本
    public event Action<bool> ConnectionChanged; // 连接状态变化
    public bool IsOpen { get; }
    public long TxBytes { get; }
    public long RxBytes { get; }
    public string PortName { get; }

    public void Open(string portName, int baudRate, ...);
    public void Close();
    public void Send(string content);
    public void SetDtr(bool enable);
    public void SetRts(bool enable);
}
```

**改动原则**：只搬家，不改逻辑。MainWindow 持有 `SerialPortSession _session`。

### 1.2 提取 DataConverter

```
新文件：DataConverter.cs

public static class DataConverter
{
    public static string BytesToHex(byte[] bytes);        // 逻辑不动
    public static byte[] HexToBytes(string str);           // 逻辑不动
    public static string BytesToText(byte[], string enc);  // 逻辑不动
    public static byte[] TextToBytes(string str, string enc); // 逻辑不动
}
```

### 1.3 提取 ProtocolParser

```
新文件：ProtocolParser.cs

public class ProtocolParser
{
    public static ParseResult Parse(string rawLine);
}

public class ParseResult
{
    public List<ProtocolMessage> Messages;  // [type, arg1, arg2, ...]
    public string PlainText;                // 方括号外的普通文本
}
```

解析规则：
- 匹配 `[...]`，支持引号包裹参数 `"..."`
- `[plot,P,0.5]` → `{Type:"plot", Args:["P","0.5"]}`
- `[display,0,0,"hello,18",24]` → `{Type:"display", Args:["0","0","hello,18","24"]}`
- 括号外的文本归入 `PlainText`

### 1.4 引入 MVVM

NuGet：`CommunityToolkit.Mvvm`

```
新建：
  ViewModels/MainViewModel.cs        - 主 VM，持有 Session + 各面板 VM
  ViewModels/PlotViewModel.cs        - 骨架
  ViewModels/SliderPanelViewModel.cs - 骨架
  ViewModels/KeyPanelViewModel.cs    - 骨架
  ViewModels/DisplayPanelViewModel.cs- 骨架
```

Phase 1 的 `MainViewModel` 只有一个 `SerialPortSession`，多串口时改为 `ObservableCollection<SerialPortSession>`。

### 1.5 偏好存储 JSON 化

```
旧：window.cfg（Key=Value 文本，不可扩展）
新：prefs.json（JSON，可存复杂结构）

{
  "window": { "left": 100, "top": 50, "width": 920, "height": 560 },
  "theme": "Dark",
  "lastPort": "COM3",
  "preferences": {
    "lineEnding": "\\r\\n",
    "autoClear": false,
    "autoReconnect": true,
    "showEcho": true,
    "showLineNumbers": true,
    "persistTraffic": false,
    "separateSystemLog": true
  },
  "quickSends": { "AT": "AT\\r\\n", ... },
  "keys": [],        // Phase 4 填充
  "sliders": [],     // Phase 4 填充
  "plotSettings": {} // Phase 3 填充
}
```

### Phase 1 验证

编译 → 双击 exe → 收发数据、主题切换、搜索、暂停、快捷发送 → 行为与 v1.0.0 完全一致。

---

## Phase 2：UI 重组

### 实施顺序（先骨架，再填肉）

```
1. MainWindow.xaml 全量重写布局（只放结构，不填内容）
   - 顶栏 + TabBar + 主区 ContentControl + 右侧面板 ContentControl + 状态栏
   - 此时所有 ContentControl 指向空占位，编译通过即可
2. 串口打开/关闭打通（顶栏按钮 → MainViewModel → SerialPortSession）
3. 标签切换逻辑（MainViewModel.CurrentTab，快捷键 Ctrl+1~6）
4. 右侧面板内容切换（每个标签一个占位 UserControl）
5. 右侧面板折叠/固定逻辑（动画 + ⚓◀ 按钮）
6. 接收区标签 → 搬 AvalonEdit + 发送区 + 全部 v1 功能保留
   （工作量最大的一步：验证 2.8 检查清单每一条）
7. 接收区右侧面板 → 填入收发高频开关
8. 绘图标签 → 空壳 View（只有顶部工具栏框架 + 空状态文字）
9. 控件标签 → 空壳 View（只有顶部工具栏框架 + 空状态文字）
10. 设置标签 → 右侧面板填全量配置 + 快捷键二级页框架
11. 编译验证 → v1 全部功能一个不漏
```

**原则**：第 1 步是整个 Phase 2 的地基——壳子能跑、标签能切、面板能切，后面全是往里填东西。

### 2.1 当前 v1 布局的问题

```
当前 v1（分析问题）：
┌──────────────────┬──────────────┐
│ 接收区           │ 串口配置     │  ← 右侧面板占 236px，挤占收发区
│  ├ 系统消息挤位 │ 接收设置     │
│  ├ AvalonEdit   │ 发送设置     │
│  └ [暂停][导出] │ 外观         │
│                  │              │
│ 发送区           │              │
│  ├ 快捷发送     │              │
│  ├ tbSend       │              │
│  └ 提示文字挤位 │              │  ← Enter发送提示占地
└──────────────────┴──────────────┘
```

三个挤占点：
1. 右侧设置面板 236px 恒定存在——配一次不碰的东西永远占着
2. 系统消息栏 36-72px 挤接收区
3. Enter 发送提示文字占一行

### 2.2 V2 布局设计（最终版）

```
┌──────────────────────────────────────────────────────┐
│ 串口 [COM3▼] [115200▼] [打开]                 v2.0.0│ ← 顶栏
├──────────────────────────────────────────────────────┤
│ [接收区] [绘图] [按键] [滑杆] [OLED]       │ [设置] │ ← 标签栏
├────────────────────────────────┬─────────────────────┤
│                                │ ⚓ 固定  ◀ 折叠     │
│  当前标签的主内容区            │                     │
│  (接收/绘图/按键...)          │  右侧面板           │
│                                │  内容跟着标签走：   │
│                                │                     │
│                                │  接收区→收发高频开关│
│                                │  绘图  →绘图设置   │
│                                │  按键  →按键属性   │
│                                │  滑杆  →滑杆属性   │
│                                │  OLED  →OLED 设置  │
│                                │  设置  →全量配置   │
│                                │   (串口/模式/主题) │
├────────────────────────────────┴─────────────────────┤
│ ● 已连接 │ COM3 @ 115200 │ TX:1.2KB RX:8.5KB       │
└──────────────────────────────────────────────────────┘
```

**和上一版的关键区别**：

| 项目 | 上一版 | 最终版 |
|------|--------|--------|
| 设置 | ⚙ 弹窗 | `[设置]` 标签（与内容标签同一行），右侧面板显示全量配置 |
| 标签栏 | 全是内容标签 | 内容标签 ... `│` `[设置]`，用竖线分隔 |
| 右侧面板 | 无 | **始终存在**，内容随标签切换 |
| 右侧面板控制 | — | ⚓ 固定（钉住） / ◀ 折叠（收起） |
| 主题切换 | 顶栏 | 设置标签的外观区 |
| 顶栏内容 | 串口+波特率+打开+⚙ | 串口+波特率+打开+版本号 |

### 2.3 右侧面板行为

```
固定宽度：220px（不可拖拽调整，后续按需加 GridSplitter）

⚓ 固定模式：面板始终展开
◀ 折叠模式：面板收起，主内容全宽，右侧露出 ▶ 按钮

默认行为（不固定也不折叠）：
  - 鼠标移到右边缘 → 面板滑出
  - 鼠标移开 → 面板自动缩回
  - 适合偶尔看一眼设置但不希望一直占地方
```

### 2.4 各标签下右侧面板的内容

| 标签 | 右侧面板显示 |
|------|-------------|
| [接收区] | **仅收发时高频开关**：时间戳格式、☑消息回显、☑行号显示、☑系统消息独立；换行符、☑定时发送+间隔、☑发送后清空 |
| [绘图] | X 轴范围（下拉+可自定义）、Y 轴（自动适配/固定范围）、☑标点、☑连线、☑数值显示、[清除] [导出CSV]。注意：⏸暂停在顶部工具栏，不在右侧面板 |
| [按键] | 正常时空白或提示"点击编辑按钮开始配置"；编辑时显示当前选中按键的属性 |
| [滑杆] | 正常时空白或提示；编辑时显示当前选中滑杆的属性 |
| [OLED] | 清屏按钮、字号预设等 |
| [设置] | **配一次基本不动的东西**：串口配置（数据位/停止位/校验位/流控/DTR/RTS）、收/发模式+编码、自动重连、流量持久化、外观（主题切换） |

### 2.5 各标签主内容区设计

#### 接收区标签

```
┌─ 接收区 ───────────────────────────┬─ 收发设置 ─────┐
│ [暂停] [导出] [清空]  [搜索 Ctrl+F]│ 时间戳 [fff▼]  │
│ ┌─ AvalonEdit ───────────────────┐ │ ☑ 消息回显     │
│ │ 14:23:05.123 -> [plot,P,0.5]  │ │ ☑ 行号显示     │
│ │ 14:23:05.145 -> [slider,kp,17]│ │ ☑ 系统消息独立 │
│ │ 14:23:05.167 -> Hello from... │ │                │
│ │ ...                           │ │ 换行符 [\r\n▼] │
│ └───────────────────────────────┘ │ ☑ 定时发送     │
│                                   │ 间隔 [1000]ms  │
│ 发送区：                          │ ☑ 发送后清空   │
│ ┌─ 快捷发送 chips ──────────────┐ │                │
│ │ [AT] [AT+CWLAP] [AT+CWMODE=1]│ │                │
│ └───────────────────────────────┘ │                │
│ ┌─ tbSend ──────────────────────┐ │                │
│ │ 输入要发送的内容…             │ │                │
│ └───────────────────────────────┘ │                │
│ Enter发送·Shift+Enter换行 [清空][发送]│             │
└──────────────────────────────────┴────────────────┘
```

所有 v1 的接收区功能完整保留：行号、系统消息独立显示、智能滚屏锁定、日志导出、时间戳、消息回显。

#### 绘图标签（右侧面板展开时）

> **布局原则**：波形图占满主区 → 示波器风格。数值不切分屏幕，以半透明 HUD 叠在波形图角落。操作按钮在顶部工具栏（统一模式）。配置项在右侧面板。

```
┌─ 绘图 ───────────────────────────┬─ 绘图设置 ──┐
│ [⏸ 暂停] [🗑 清除] [📥 CSV] [🔍 适应]          │              │  ← 顶部工具栏
│ ┌─ OxyPlot ────────────────────┐ │ X 轴范围     │
│ │                    ┌───────┐ │ │ [30s     ▼]  │
│ │  ╱╲    ╱╲          │●P 0.52│ │ （10/30/60/  │
│ │ ╱  ╲  ╱  ╲         │●I 0.18│ │  120/300s+   │
│ │ ╱    ╲╱    ╲___     │●D-0.34│ │  自定义输入） │
│ │                     └───────┘ │              │
│ │  ── P  ── I  ── D  ▲数值HUD │ Y 轴         │
│ │  ▲ OxyPlot原生图例 半透明底 │ ○ 自动适配   │
│ │    在图上，点击切换显隐    │ ○ 固定范围   │
│ │                           │ [-1.0]~[1.0]  │
│ │                           │              │
│ │                           │ 显示选项     │
│ │                           │ ☑ 标点       │
│ │                           │ ☑ 连线       │
│ │                           │ ☑ 数值显示   │
│ │                           │              │
│ │                           │ [清除]       │
│ └───────────────────────────┘ │ [导出 CSV]   │
└───────────────────────────────┴──────────────┘
```

**设计要点**：
- **波形图**：OxyPlot 填满顶部工具栏以下、底部以上的全部空间。波形图是第一公民
- **数值 HUD**：半透明叠加层（`Background="#AA1E1E1E"`，圆角 6px），固定在波形图右上角（Margin 8px），`IsHitTestVisible="False"` 鼠标穿透。每行 = 曲线颜色圆点(8px) + 曲线名 + 最新值右对齐，等宽字体。右侧面板 ☑数值显示 控制开关。不是弹窗、不可拖拽、不需要关闭——就是图上的一层信息
- **图例**：OxyPlot 原生图例，`LegendPosition = LeftTop`，与右上角数值 HUD 不冲突。点击图例项可切换曲线显隐
- **标点交互**：暂停后鼠标悬停 → 垂直十字光标 + tooltip 显示所有曲线在该时刻的精确值。点击可钉住标记
- **暂停/清除/CSV/适应**：在顶部工具栏（操作型按钮），不在右侧面板（右侧面板只放配置）
- **快捷键**：Space = 暂停/恢复，Ctrl+K = 清除波形

#### 绘图标签（右侧面板收起时）

```
┌─ 绘图 ──────────────────────────────────────────┐ ▶
│ [⏸ 暂停] [🗑 清除] [📥 CSV] [🔍 适应]          │
│ ┌─ OxyPlot 全宽 ──────────────────────────────┐ │
│ │                                   ┌───────┐ │ │
│ │  ╱╲    ╱╲    ╱╲                   │●P 0.52│ │ │
│ │ ╱  ╲  ╱  ╲  ╱  ╲                  │●I 0.18│ │ │
│ │ ╱    ╲╱    ╲╱    ╲                 │●D-0.34│ │ │
│ │        ╲       ╱                  └───────┘ │ │
│ │         ╲_____╱                             │ │
│ │                                              │ │
│ │  ── P  ── I  ── D                           │ │
│ └──────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────┘
```

收起时波形图横铺全宽，数值 HUD 位置不变（相对于 OxyPlot 右上角），▶ 按钮在右侧边缘供恢复面板。不需要把信息挪到图下面——HUD 已经在图上了。

**⏸ 暂停按钮 Toggle 态**：
- 运行中：默认按钮背景 + ⏸ 图标 + 文字"暂停"
- 暂停中：AccentBrush 半透明背景（高亮）+ ▶ 图标 + 文字"继续"
- 按钮固定宽度（避免文字切换导致工具栏按钮跳动）
- Space 键等效切换

**绘图空状态**：
- 串口未连接时：OxyPlot 空坐标轴 + 半透明灰色文字"串口未连接"叠加居中
- 串口已连接但无数据：文字改为"等待串口数据…"
- 收到第一个 `[plot,...]` 后文字消失
- 点击清除后文字重新出现
- 文字样式：和数值 HUD 同风格（半透明底 + IsHitTestVisible=False）

#### 控件标签（以按键为例）

```
正常模式：
┌─ 按键 ───────────────────────────┬────────────────┐
│ [编辑]                           │ (点击编辑按钮  │  ← 顶部工具栏
│ ┌─ 内容区 ─────────────────────┐ │  开始配置按键) │
│ │ [MyDownKey] [StartMotor]     │ │                │
│ │ [StopMotor]                  │ │                │
│ │                              │ │                │
│ └──────────────────────────────┘ │                │
└──────────────────────────────────┴────────────────┘

编辑模式（点了 [编辑]）：
┌─ 按键 ───────────────────────────┬─ 按键属性 ─────┐
│ [完成] [+ 添加]                  │ 名字:          │  ← 顶部工具栏切换
│ ┌─ 内容区 ─────────────────────┐ │ [MyDownKey   ] │
│ │ [MyDownKey] [StartMotor]     │ │                │
│ │ [StopMotor]                  │ │ 发送模式:      │
│ │                              │ │ [文本 ▼]       │
│ │ ↑ 点某个按键，右侧显示其属性 │ │                │
│ │                              │ │ 发送值:        │
│ │                              │ │ [down        ] │
│ │                              │ │                │
│ │                              │ │ ☐ 锁定         │
│ │                              │ │                │
│ └──────────────────────────────┘ │ [删除]         │
└──────────────────────────────────┴────────────────┘
```

编辑模式逻辑：
- `[编辑]` / `[完成]` 按钮在**主内容区顶部工具栏**（不在底部、不在右侧面板），和所有标签的操作按钮统一位置
- 进入编辑后，右侧面板切换为该控件类型的属性编辑器
- 主内容区点哪个控件 → 右侧面板显示该控件的属性
- 点 `[完成]` → 保存配置到 JSON，右侧面板退出编辑状态
- 所有控件类型（按键/滑杆/OLED/摇杆）共用这套模式，区别只是属性字段不同

**控件空状态**：
- 用户尚未创建任何控件时，主内容区中央显示灰色提示文字
- 统一模板：`点击 [编辑] 开始添加XX`，`[编辑]` 文字用强调色（AccentBrush）兼作视觉引导
- 各控件变体：`点击 [编辑] 开始添加按键` / `点击 [编辑] 开始添加滑杆` / `点击 [编辑] 开始添加 OLED`

**控件编辑选中高亮**：
- 未选中：常态外观
- 悬停：底色微变（+5%亮度或 AccentHoverBrush 半透明叠加）
- 选中：AccentBrush 2px 边框 + 底色微亮
- 所有控件类型（按键/滑杆/摇杆/OLED）共用这套选中态，和 TouchGFX 淡蓝色选中标识一致

**键盘布局预设**（按键标签编辑模式下可用）：
- 右侧面板底部放 `[自定义按键布局]` 入口
- 点击展开预设布局列表：`键盘布局`、`方向键布局`、`数字键盘布局`（Phase 4 可扩展更多）
- 键盘布局基于 Windows 虚拟键盘排列（QWERTY 标准布局），含大小写切换键 ⇧
- 点击预设 → 主内容区按布局批量创建按键，位置排列固定（不可拖拽），右侧面板保持属性编辑状态
- 预设自动填入：名字=键位标签（如 `K`），发送模式可选 HEX/文本/数据包（预设为数据包），发送值=键位字符（小写模式默认）
- ⇧ 大小写切换：键盘布局左上角有 ⇧ 键，点击切换大小写模式 → 再点其他键即按当前大小写模式创建
- 用户可在此基础上逐个修改任意按键的发送模式/发送值/锁定状态
- 用户自定义布局可保存到 `prefs.json`，名称由用户指定

### 2.6 [设置] 标签

点 `[设置]` 标签时，主内容区保持之前的内容不变，右侧面板切换到全量配置。

**分组方式**：滚动页面 + 细分隔线（不用折叠手风琴）。设置项共约 14 行，一屏可见，扫一眼找到改一下。

```
┌─ （主内容区不变）───────────────┬─ 设置 ──────────┐
│                                  │ 串口配置        │  ← 分组标题灰色加粗
│       之前在看什么就还是什么      │  数据位 [8 ▼]   │
│       不受影响                   │  停止位 [1 ▼]   │
│                                  │  校验位 [无 ▼]  │
│                                  │  流控   [无 ▼]  │
│                                  │  ☑ DTR  ☑ RTS │
│                                  │ ─────────────── │  ← 细分隔线
│                                  │ 接收设置        │
│                                  │  接收模式[文本▼]│
│                                  │  接收编码[UTF-8]│
│                                  │ ─────────────── │
│                                  │ 发送设置        │
│                                  │  发送模式[文本▼]│
│                                  │  发送编码[UTF-8]│
│                                  │ ─────────────── │
│                                  │ 外观            │
│                                  │  🌙/☀ 主题      │
│                                  │ ─────────────── │
│                                  │ ☑ 自动重连      │
│                                  │ ☑ 流量持久化    │
│                                  │ [⚷ 快捷键提示]  │  ← 进入二级页
└──────────────────────────────────┴─────────────────┘
```

**注意**：时间戳、消息回显、行号显示、系统消息独立、换行符、定时发送、发送后清空——这些**不在设置里**，在接收区右侧面板（因为只在收发时用到）。

**设计意图**：用户在绘图标签看波形图时想改波特率 → 点 `[设置]` → 右侧面板出现串口配置 → 改完 → 点回 `[绘图]` → 右侧面板切回绘图设置。主内容区自始至终是波形图，操作不中断。

**快捷键提示**（设置面板内的二级页）：
- 设置面板底部放 `[⚷ 快捷键提示]` 入口
- 点击后→右侧面板内容推入子页面，标题变为 `← 快捷键`，显示所有快捷键分组（全局 / 接收区 / 绘图 / 控件）
- 点 `←` 返回设置主页面
- 不弹窗、不打开网页、不打断主区内容

```
设置主页面：                    快捷键二级页：
┌─ … ─┬─ 设置 ──────┐          ┌─ … ─┬─ ← 快捷键 ──┐
│     │ 串口配置     │          │     │ 全局         │
│     │   ...        │          │     │ Ctrl+1 接收区 │
│     │ ──────────── │   点     │     │ Ctrl+2 绘图   │
│     │ [⚷ 快捷键]   │ ───→   │     │ Ctrl+0 设置   │
│     │              │          │     │              │
│     │              │          │     │ 绘图         │
│     │              │          │     │ Space 暂停    │
│     │              │          │     │ Ctrl+K 清除   │
│     │              │          │     │              │
│     │              │          │     │ 接收区       │
│     │              │          │     │ Ctrl+F 搜索   │
│     │              │          │     │ ...           │
└─────┴──────────────┘          └─────┴──────────────┘
```

### 2.7 标签栏行为

- `│` 竖线左边：内容标签（[接收区]/[绘图]/[按键]/[滑杆]/[OLED]）
- `│` 竖线右边：[设置]（独占、始终在最后）
- 内容标签中：接收区和绘图预置始终存在；按键/滑杆/OLED/摇杆动态出现（用户添加控件后自动出现，删光自动隐藏）
- 超出宽度可水平滚动（鼠标拖拽或滚轮）
- 快捷键：`Ctrl+1` 接收区、`Ctrl+2` 绘图、`Ctrl+3~6` 动态标签、`Ctrl+0` 设置

### 2.8 v1 功能保留检查清单

Phase 2 实施过程中，以下 v1 功能必须一个不漏地保留：

```
接收区：
  ☑ 行号显示 + 开关
  ☑ 系统消息独立显示 + 开关 + 淡入淡出
  ☑ 时间戳三格式
  ☑ 消息回显开关
  ☑ 暂停显示 + 缓冲 + 满提示
  ☑ Ctrl+F 搜索（关键字高亮/正则/大小写敏感）
  ☑ 智能滚屏锁定（≤8px）
  ☑ 日志导出
  ☑ 清空接收区

发送区：
  ☑ 快捷发送面板 + 右键编辑删除 + 预置 AT 指令
  ☑ 发送历史下拉（最近 20 条去重）
  ☑ HEX 实时格式化
  ☑ 换行符可选
  ☑ 定时发送 + 间隔输入
  ☑ 发送后自动清空
  ☑ Enter=发送 / Shift+Enter=换行
  ☑ Placeholder 水印

基础：
  ☑ 暗色主题一键切换
  ☑ 热插拔检测 + 自动重连
  ☑ 流量统计 + 右键重置 + 持久化
  ☑ 快捷键 Ctrl+Enter/L/P/Shift+L
  ☑ 启动自动选串口
  ☑ 5 层异常分类
  ☑ 崩溃日志
  ☑ 串口参数全部（流控/DTR/RTS/自定义波特率）
  ☑ 21 色 DynamicResource 设计系统
  ☑ 9 套 ControlTemplate
```

### 2.9 Phase 2 产出

```
改动文件：
  MainWindow.xaml           — 全量重写布局（保留 21 色系统 + 全部 ControlTemplate）
  MainWindow.xaml.cs        — 配合新布局调整事件处理
  MainViewModel.cs          — 标签切换 + 右侧面板内容切换
  KeyPanelViewModel.cs      — 编辑模式骨架
  SliderPanelViewModel.cs   — 编辑模式骨架
```

**验证**：编译 → 双击 → 顶栏串口选择/打开 → 标签栏切换 → 右侧面板随标签变化 → 收发数据正常 → 主题切换正常 → 所有 v1 功能一个不漏。

---

## Phase 3：波形图面板

### 实施顺序（先画图，再加肉）

```
1. NuGet 引入 OxyPlot.Wpf
2. PlotViewModel 实现：PlotModel + Dictionary<string, LineSeries> + OnPlotMessage
   → 验证 OxyPlot 在 WPF 里能正常渲染一根静态线
3. PlotView.xaml：Grid 里放 PlotView + 数值 HUD Border（半透明、鼠标穿透）
4. 顶部工具栏：[⏸ 暂停] [🗑 清除] [📥 CSV] [🔍 适应]
   → ⏸ 暂停 Toggle 态做 DataTrigger（图标+文字+背景色三切换）
5. 右侧面板：X 轴范围 / Y 轴自动-固定 / ☑标点 / ☑连线 / ☑数值显示 / [清除] / [CSV]
6. 绘图空状态（"等待串口数据…"叠加文字）
7. ProtocolParser → PlotViewModel 数据流打通
   → 串口收到 [plot,P,0.5] → 曲线追加数据点 → 波形滚动
8. 暂停后十字光标 + tooltip（标点交互）
9. 编译验证 → STM32 发 plot 数据 → 波形出现 + HUD 更新
```

**原则**：第 2 步验证 OxyPlot 能跑，之后全是增量。不要先建完整 UI 再试 OxyPlot——如果 OxyPlot 有兼容问题（概率极低），早发现早处理。

### 3.1 技术选型

**OxyPlot.Wpf**（NuGet）

### 3.2 实现

**PlotViewModel**：
```csharp
public class PlotViewModel : ObservableObject
{
    public PlotModel Model { get; } = new PlotModel();
    private Dictionary<string, LineSeries> _series = new();
    private const int MaxPoints = 5000;

    public void OnPlotMessage(string name, double value, DateTime timestamp)
    {
        if (!_series.TryGetValue(name, out var series))
        {
            series = new LineSeries { Title = name };
            Model.Series.Add(series);
            _series[name] = series;
        }
        series.Points.Add(new DataPoint(DateTimeAxis.ToDouble(timestamp), value));
        if (series.Points.Count > MaxPoints) series.Points.RemoveAt(0);
        Model.InvalidatePlot(true);

        // 更新数值面板
        UpdateValueDisplay(name, value);
    }
}
```

### 3.3 交互

> 完整布局设计见 2.5 绘图标签。以下为功能实现要点。

| 功能 | 实现 |
|------|------|
| 缩放平移 | OxyPlot 内置鼠标滚轮缩放 + 右键拖拽平移 |
| 图例 | `LineSeries.Title`，OxyPlot 原生图例（`LegendPosition = LeftTop`），点击切换曲线显隐 |
| 数值 HUD | 半透明叠加层固定在波形图右上角（`IsHitTestVisible="False"`），每行 = 颜色圆点 + 曲线名 + 最新值（等宽字体）。右侧面板 ☑数值显示 开关控制。不是弹窗、不可拖拽 |
| 暂停绘图 | 顶部工具栏 ⏸ 按钮（Toggle），串口继续收但曲线不追加（`_paused` 标记）。Space 键同效 |
| 标点/十字光标 | 暂停后鼠标悬停 → 垂直十字光标 + 浮动 tooltip 显示所有曲线在该 X 位置的精确值。点击可钉住标记 |
| 清除 | 顶部工具栏 🗑 按钮，清空所有曲线系列。Ctrl+K 同效 |
| 适应窗口 | 顶部工具栏 🔍 按钮，重置缩放/平移到默认视图 |
| 导出 CSV | 顶部工具栏 📥 按钮（快速入口）。右侧面板也保留一份（低频入口）。遍历所有系列写文件 |

### Phase 3 验证

STM32 发 `[plot,P,%f][plot,I,%f][plot,D,%f]` → 图形出现 P/I/D 三曲线 + 数值面板更新。

---

## Phase 4：控件面板

### 实施顺序（一个控件一个控件做，每做一个验证一个）

```
按键面板（最复杂，先做——做通了后面的复用模式）：
  1. KeyViewModel: Name / SendMode / SendValue / IsLocked / Width / Height / Color
  2. 基本编辑模式（添加/删除/属性编辑/选中高亮 AccentBrush 边框）
  3. Ctrl+多选 + 批量改（ListBox SelectionMode="Extended"）
  4. 按键颜色 → 右侧面板预设色块点选
  5. 按键大小 → 右侧面板 Width/Height 数值调整（WrapPanel 自动排列）
  6. 键盘布局预设 — QWERTY + ⇧ 切换
  7. 方向键 + 数字键盘预设
  8. 自定义布局保存/加载（prefs.json）
  9. STM32 收发联调

滑杆面板（复用按键的编辑模式框架）：
  1. SliderViewModel: Name / Min / Max / Step / SendInterval
  2. Slider 控件 + 拖拽节流（200ms 防抖发送）
  3. 编辑模式 + 属性面板
  4. STM32 收发联调

OLED 面板（Canvas 渲染，工作量中等）：
  1. DisplayViewModel + Canvas 渲染
  2. display-clear 清屏
  3. 字号/颜色设置
  4. STM32 收发联调

摇杆面板（最低优先级）：
  1. JoystickViewModel + 双圆座 Canvas
  2. 鼠标拖拽 + 回控节流
  3. 编辑模式 + 属性面板
  4. STM32 收发联调
```

**原则**：按键面板是控件里最复杂的——它做通了，编辑模式框架、选中高亮、Ctrl+多选、属性面板切换、JSON 持久化这套流水线就全部验证过了。滑杆/OLED/摇杆只需要换个 View 和几个属性字段，模式完全复用。

### 4.1 通用编辑模式

所有控件面板共用一套编辑逻辑（Phase 2 已在 UI 层实现，Phase 4 只需填充各控件的属性字段）：

```
每个控件面板的主内容区顶部工具栏有 [编辑] 按钮：
  点 [编辑] → 顶部工具栏变为 [完成] [+ 添加]
             右侧面板切换为该控件类型的属性编辑器
  点某个控件 → 右侧面板显示该控件的属性
  改属性 → 实时反映到 JSON 配置
  点 [完成] → 保存到 prefs.json，右侧面板退出编辑状态

一次只能一个面板在编辑。
```

编辑时右侧面板显示的属性字段：

| 控件类型 | 属性字段 |
|---------|---------|
| 按键 | 名字、发送模式(文本/HEX/固定包)、发送值、锁定 ☐ |
| 滑杆 | 名字、最小值、最大值、分辨率、发送间隔(ms) |
| OLED | （暂无控件级属性，清屏按钮即可） |
| 摇杆 | 编号、各轴范围、回控间隔

### 4.2 按键面板

```
协议：[key,名字,状态]    如 [key,MyDownKey,down]

按键属性：
  名字: MyDownKey
  发送模式: 文本 / HEX / 固定数据包
  发送值: down（或 0x01，或 [key,MyDownKey,down]）
  锁定: □（锁定后只显示不收不发）

UI（手动模式）：
  方形按钮排列，收到 down 高亮，收到 up 恢复
  点击按钮 → 按配置的发送模式发出

按键大小三层方案：
  ① 键盘布局预设 → 固定大小（模板定义，如 QWERTY 键 36×28，方向键 48×48）
  ② 手动创建的按键 → 右侧面板编辑 Width/Height 数值（非鼠标拖拽手柄）
     例如默认 80×36，做大红色急停按钮 → 右侧面板改成 160×64
     排列方式：WrapPanel 自动流式排列（不做 Canvas 自由定位）
  ③ 按键颜色 → 右侧面板预设色块点选（红/绿/蓝/黄/白/灰）
     对应 v1 系统色的危险/成功/信息语义，不做自由取色器

多选批量编辑（Ctrl+Click）：
  编辑模式下 ListBox 支持 Ctrl+Click 多选
  多选时右侧面板显示 "已选 N 个按键"
  修改发送模式/发送值/颜色 → 写入全部已选按键
  名称不改（保持各自独立）

键盘布局预设：
  编辑模式下，右侧面板底部放 [自定义按键布局] 入口
  点击展开预设列表：
    · 键盘布局（QWERTY 标准，含 ⇧ 大小写切换键）
    · 方向键布局（↑↓←→）
    · 数字键盘布局（3×4 小键盘，含 Enter）
    · （Phase 4+ 可扩展更多预设）

  键盘布局工作流程：
    1. 用户点击 [键盘布局] → 主内容区按 QWERTY 排列批量创建按键
       （参考 Windows 虚拟键盘布局，非 108/86 键全键盘）
    2. 布局左上角有 ⇧ 大小写切换键（ToggleButton 风格）
       · 默认小写模式：点击 K 键 → name="K", sendValue="k"
       · 切换大写模式：点击 K 键 → name="K", sendValue="K"
       · ⇧ 切换只影响随后点击的键，不影响已创建的键
    3. 预设填充的按钮统一按一种发送格式（默认"数据包"）
       · 用户可逐个选中后修改发送模式/发送值/锁定
    4. 用户自定义的键盘布局可保存到 prefs.json（名称自定义）
       · 每个保存的布局记录：键位排列 + 各键的独立发送配置
    5. 键盘布局模式下按键位置固定（不可拖拽），避免误移
       · 退出编辑后布局仍在主区正常显示和工作
```

```
协议：[slider,名字,百分比]    如 [slider,kp,17.0]

滑杆属性：
  名字: kp
  最小值: 0
  最大值: 100
  分辨率: 0.1（小数位数自动推断或手动设置）
  发送间隔: 200ms（拖拽时防抖动）

回控：用户拖滑杆 → 节流发送 [slider,kp,新值]
```

### 4.4 虚拟 OLED

```
协议：[display,x,y,文本,字号]    文本含逗号时用引号包裹
      [display-clear]            清屏

UI：深色底 + 亮色字的位图屏幕，Canvas 渲染
```

### 4.5 摇杆面板（最低优先级）

```
协议：[joystick,编号,h1,v1,h2,v2]
UI：双摇杆圆座 + 鼠标拖拽
```

---

## Phase 5：多串口

`SerialPortSession` 可实例化 → `ObservableCollection<SerialPortSession>` → `TabControl` 绑定 → 每个串口独立。

**注意**：当前不是刚需。架构预留，需要时加。

---

## Phase 6：.NET 8 升级 + 打磨

```
- .csproj：net48 → net8.0-windows
- OxyPlot 升到最新版
- 单文件发布
- 响应式适配
- 快捷键扩展
```

---

## 关键技术决策（最终版）

| # | 问题 | 决策 |
|:--:|------|:---:|
| 1 | MVVM | CommunityToolkit.Mvvm |
| 2 | 图表库 | OxyPlot.Wpf |
| 3 | 偏好存储 | JSON（`prefs.json`） |
| 4 | 串口层 | System.IO.Ports |
| 5 | 渲染引擎 | AvalonEdit（不变） |
| 6 | 布局 | 顶栏 + 标签栏 + 全宽主内容 + 右侧面板 |
| 7 | 标签栏 | 内容标签 ... `│` `[设置]`，设置独占最右，竖线分隔 |
| 8 | 右侧面板 | 始终存在，内容随标签切换；⚓固定 / ◀折叠 |
| 9 | 设置入口 | `[设置]` 标签（非弹窗），右侧面板显示全量配置 |
| 10 | 主题切换 | 在设置标签的外观区 |
| 11 | 顶栏内容 | 串口号 + 波特率 + 打开按钮 + 版本号（不放设置齿轮） |
| 12 | 控件编辑 | 统一模式：[编辑]在主内容区工具栏，右侧面板变属性编辑器 |
| 13 | 按键发送 | 文本/HEX/固定数据包 三选一 + 锁定 |
| 14 | display 文本 | 含逗号时引号包裹 `"hello,18"` |
| 15 | 标签栏动态 | 接收区/绘图预置始终存在；按键/滑杆/OLED/摇杆按需出现 |
| 16 | 标签超出宽度 | 水平滚动（鼠标拖拽或滚轮） |
| 17 | .NET 目标 | 先 4.8，Phase 6 升 8 |
| 18 | v1 功能保留 | Phase 2 附完整检查清单，一个不漏 |
