# 用户手册决策记录

> 和 Encaron 逐条讨论，确认一条锁一条。锁了就不再推翻。
> 2026-06-22 状态：✅ 中文版已完成，⬜ 英文翻译待做

## ✅ 已锁 — 全部 9 条

### 1. 帮助按钮样式
`?` 圆圈按钮 → 顶栏 [中/EN] [🌙] ** [?] ** [v2.6.5]
20×20 圆 Border，Click → `Process.Start("index.html")`
✅ 已实现（XAML + Theme.cs + csproj 发布拷贝）

### 2. 手册载体
HTML，源码 `Serial Monitor V2\用户手册\`，发布时 csproj 自动拷到 exe 旁
零依赖，纯 HTML + CSS + 原生 JS

### 3. 内容来源
repo README + Serial_C_Language/README.md + example/README.md
所有外部链接指向 GitHub（用户下安装包没有本地仓库路径）

### 4. 页面结构（已变更为三页）
```
index.html     → 落地页（Hero 分栏 + 特性卡片 + 面板卡片 + 截图展示 + CTA + 页脚）
manual.html    → 主手册（侧栏三级树 + 9 面板详情 + 协议速查 + 设置页 + MCU 对接 + FAQ）
oled.html      → OLED 深度文档（MCU 解析全代码：GetField/路由/图形处理/F5/双屏/旋转/橡皮擦）
```

### 5. 整体风格
暗色科技风（GitHub-dark），CSS `prefers-color-scheme` 跟随系统
Hero：网格背景 + 3 浮动光球 + 12 颗粒子呼吸闪烁
侧栏：三级树结构（分组 > 标签页 > 子区域），折叠箭头 + 自动展开 + 滚动可见

### 6. 内容结构
- 落地页：Hero（左文右图）+ 6 特性卡片 + 8 面板卡片 + 4 张截图展示 + CTA + 页脚
- 主手册：快速入门 + 9 面板详情（收发/波形/FFT/传感/按键/滑杆/摇杆/OLED）+ 协议大表 + 设置页 + MCU 对接 + FAQ
- OLED 深度：系统架构 + 协议参考 + GetField + type 路由 + 图形处理（LCD 彩色版为主）+ F5 增量同步 + LCD 端显示 + 旋转 + 橡皮擦

### 7. 帮助按钮
和手册一起上线 ✅ 已编译发布

### 8. 语言策略
先中文，后英文。`manual.js` 已预留 `I18N` 对象（~70 key，`zh` 全填，`en` 全空）。
语言切换按钮已写但 `display:none`。英文补完后打开。

### 9. 截图
亮色模式下截取，PNG。暗色用 CSS `filter: brightness(0.85)` 压暗。
未来补暗色截图时改用 `<picture>` 双源 + 删 CSS filter。
共 **20 张**，全部已放入 `images/`。

---

## 当前文件清单

```
Serial Monitor V2\用户手册\
├── index.html          (176 行)  落地页
├── manual.html         (~430 行) 主手册
├── oled.html           (521 行)  OLED 深度文档
├── manual.css          (~550 行) 暗色科技风 + 亮暗跟随 + 侧栏三级树 + 语法高亮 + 动效
├── manual.js           (~270 行) I18N 双语预留 + 复制按钮 + 滚动进度 + 侧栏折叠/展开
├── 用户手册决策.md     本文件
├── images/             (20 张 PNG + 1 张 JPG)
│   ├── logo.png
│   ├── overview.png / serial-connected.png
│   ├── plot-waveform.png / plot-detail.png / plot-tuning.png
│   ├── plot-fft.png / plot-fft-options.png
│   ├── sensor-fire.png / sensor-edit.png
│   ├── keys-panel.png / keys-edit.png
│   ├── sliders-panel.png / sliders-edit.png
│   ├── joystick-panel.png
│   ├── oled-canvas.png / oled-mcu.jpg
│   ├── settings-about.png
│   └── cubemx-nvic.png / cubemx-usart.png
└── 关于图片.md         (用户的截图备注)
```

## 唯一待做：英文翻译

1. `manual.js` — `I18N` 对象 `en: ''` 全部填空（~70 个 key）
2. 三个 HTML 文件的文字内容加 `data-i18n` 属性（当前中文硬编码在 HTML 里）
3. 语言切换按钮 `#lang-toggle` 从 `display:none` → `display:block`
4. 翻译策略：`zh` 填好了直接参考；`en` 翻译完后 `applyLang('en')` 全文瞬切
