# 串口助手 WPF — 当前状态（截至 2026-06-10，32 commits）

> **用途**：新对话快速恢复上下文。读此文件即可开始后续工作。
> **操作指南**：请主要阅读 `串口助手-WPF/` 下的文件，设计改动仅限 XAML 和 UI 相关 C#，**不要动** 4 个数据转换方法和串口核心逻辑。

---

## ⚠️ 硬性约束（每次改动前必读）

| # | 规则 | 说明 |
|---|------|------|
| **1** | **串口逻辑不可动** | 原始代码来自 B 站 STM32 教学 UP 主**江科大**，`BytesToHex` / `HexToBytes` / `BytesToText` / `TextToBytes` 以及 `OpenSerialPort` / `CloseSerialPort` / `serialPort_DataReceived` 的字节读取部分，**禁止修改** |
| **2** | **只改 UI 层面** | XAML 布局、样式、颜色、字体、动画、日志系统（`LogSystem` / `LogSent` / `LogReceived` / `AppendColoredLine` / `receiveLineBuffer` / `flushTimer`）可以改；新增控件可以加；**核心逻辑一行都不准动** |
| **3** | **用户是新手小白** | 每次改动结束必须编译成 `.exe` 放到项目根目录，用户只双击 `串口助手 WPF.exe` 运行；不要假设用户会开 VS2022、会命令行、会配置环境 |
| **4** | **Git 仅用于本地备份** | 仓库在本地，**不上传 GitHub / Gitee / 任何云端**；commit message 用中文，每次大改动前后各存一个 commit 方便回滚 |
| **5** | **前端设计 skills 可用** | 用户安装了多个前端设计 skills（design-taste-frontend、ui-ux-pro-max、minimalist-ui、industrial-brutalist-ui、antfu、vue 生态等），虽然大多是 Web 方向，但设计思维、代码规范、间距系统、色彩语义化、动效克制等理念对 WPF XAML 设计有借鉴价值 |

---

## 项目背景

### 起源
- 原始项目：**B 站 UP 主江科大**编写的 **C# WinForms 串口助手 V1.1**（位于 `串口助手/`，不动）
- 江科大的串口逻辑部分完成度极高，是本项目的基石
- 用户做了两轮修改：默认参数调整（115200/文本/UTF-8）+ WinForms 界面美化

### 为什么又开了 WPF 版
- WinForms 天花板太低（无圆角、无动画、无原生阴影）
- Web 前端不可行（浏览器不能直接访问串口），WPF 是最佳折中——串口逻辑 100% 复用、UI 脱胎换骨
- `串口助手-WPF/` 与 WinForms 原版 **并存、互不影响**

### 目标框架
- **.NET Framework 4.8**，VS2022 编译，双击 .exe 即用

---

## 文件结构

```
1111/
├── 串口助手/                          ← WinForms 原版（不动）
│   ├── 串口助手 V1.1.exe
│   ├── 串口助手-工程源码/
│   ├── 修改说明.md
│   └── 当前状态.md
│
├── 串口助手-WPF/                      ← WPF 新版（当前工作区）
│   ├── 串口助手 WPF.exe               ← 双击运行
│   ├── 串口助手-WPF.csproj            ← 项目文件 (.NET 4.8)，含 ApplicationIcon
│   ├── icon.ico                       ← exe 图标（TerminalSerial.ico）
│   ├── App.xaml / App.xaml.cs         ← 应用入口
│   ├── MainWindow.xaml                ← 界面布局 + 全量样式（含暗色主题系统）
│   ├── MainWindow.xaml.cs             ← 串口逻辑 + 日志系统 + 动画 + 主题切换
│   ├── Properties/AssemblyInfo.cs
│   ├── bin/Release/                   ← 编译输出
│   └── （你正在读的文件）
│
├── TerminalSerial.ico                 ← 图标源文件（英文名避免中文路径问题）
└── .gitignore
```

---

## Git 历史（32 个 commit，从旧到新）

| Commit | 内容 |
|--------|------|
| `4a1bf79` | 串口助手 V1.1 工程源码（WinForms） |
| `f363898` | 默认参数：115200 + 文本模式 + UTF-8 |
| `22ce8f6` | WinForms 界面美化 |
| `3ff9f25` | WPF 初版：圆角卡片、扁平按钮、状态栏 |
| `1291023` | RichTextBox 彩色日志、时间戳、消息回显、发送区自增长、字体 12pt、图标 |
| `2f15da3` | 行缓冲；Enter=发送/Shift+Enter=换行 |
| `58eb56f` | 提示文字与按钮同行 |
| `23ed411` | docs: 完整当前状态文档 |
| `f947c09` | docs: 补充硬性约束 |
| `165674b` | **refactor: 设计系统扫尾——消除全部硬编码色值 + 间距统一4px基准 + 卡片阴影** |
| `4e82740` | **feat: 状态过渡动画(280ms圆点渐变+按钮脉冲) + 智能滚屏锁定 + 发送历史下拉(最近20条)** |
| `b301e6c` | **fix: Enter发送——KeyDown→PreviewKeyDown拦截隧道事件** |
| `40d3867` | **feat: 更纱黑体检测(状态栏琥珀色提示) + 发送区placeholder水印 + 接收区日志导出** |
| `19c0139` | **feat: 时间戳格式可选(不显示/HH:mm:ss/HH:mm:ss:fff) + 定时发送(可配间隔ms)** |
| `2da92ae` | **feat: 窗口位置大小记忆(存%LocalAppData%) + GridSplitter左右面板可拖拽(200~400px)** |
| `66e46d5` | **feat: 暗色主题——状态栏🌙按钮一键切换亮/暗色板，21色自动映射，偏好记忆** |
| `6a678e0` | fix: 主题切换按钮加大(30x22)/加边框/加深前景色 |
| `9f2ca72` | fix: ComboBox下拉+ContextMenu+ScrollViewer暗色适配 |
| `d0f8bd9` | fix: .csproj加ApplicationIcon使exe文件显示自定义图标 |
| `4655e02` | assets: 更换exe图标为终端串口.ico |
| `edbf317` | fix: 图标改名TerminalSerial.ico避免中文路径问题 |
| `e642a85` | docs: 更新当前状态.md——完整记录19个commit、暗色主题系统、新增功能清单、设计系统 |
| `0cb32fd` | **feat: 流量统计——TX/RX字节计数+状态栏显示+右键重置** |
| `16e4b0d` | **feat: 快捷发送按钮面板——chip风格按钮+预设AT指令+右键编辑删除+配置持久化** |
| `2326dfc` | **feat: HEX输入实时格式化——自动过滤非法字符+每两字节插空格+转大写+光标位置保持** |
| `8519b9b` | **fix: 撤销搜索功能(RichTextBox闪退bug)+恢复对话框高度修复** |
| `80de59a` | **feat: 接收区行号显示——内嵌式右对齐5位+灰色Run+CheckBox开关+清空时重置计数** |
| `7fbb35c` | **feat: 换行符可选+发送后自动清空+自动重连+流量计数持久化** |
| `ee00923` | **feat: 系统消息独立显示——接收区上方ListBox(2行高可滚动)+CheckBox开关+偏好持久化** |
| `9b89446` | style: 系统消息框配色区分——SecondaryHoverBgBrush底+左侧3px边框 |
| `e168623` | **feat: 系统消息框切换加淡入淡出动画(200ms Opacity)** |
| `4e506aa` | **feat: CheckBox→滑动开关(ToggleSwitch)——40×22圆角滑轨+白圆钮+200ms滑入滑出动画** |
| `df2ec97` | fix: 滑动开关——圆钮改用Canvas.Left定位+Trigger同步初始位置 |
| `83fbdb2` | fix: 消息回显+行号显示加入偏好持久化，所有CheckBox统一记忆 |

> **关键差异**：`git diff 80de59a 83fbdb2` 可回顾全部本轮 UI 改动。`git diff edbf317 83fbdb2` 看全部 11 个 commit。

---

## 当前功能清单

### ✅ 已完成

#### 串口核心（从 WinForms Form1.cs 直接移植，逻辑零改动）
- 串口参数配置：串口号（动态扫描）、波特率（默认 115200）、数据位（8）、停止位（1）、校验位（无）
- 打开/关闭串口、USB 热插拔检测（WM_DEVICECHANGE）
- 接收模式：HEX / 文本模式（默认文本）
- 发送模式：HEX / 文本模式（默认文本）
- 编码：GBK / UTF-8（默认 UTF-8）
- 4 个数据转换方法：`BytesToHex` / `HexToBytes` / `BytesToText` / `TextToBytes`（**切勿改动**）
- 接收字节缓冲区：跨 DataReceived 拼接多字节字符

#### 布局 & 面板
- **GridSplitter 可拖拽分栏**：左侧收发区 + 右侧设置面板（200~400px）
- **窗口位置/大小记忆**：`%LocalAppData%\串口助手WPF\window.cfg` 存取 Left/Top/Width/Height/State/Theme
- 卡片：白底圆角 `CornerRadius=6` + `DropShadowEffect` 极淡阴影
- 间距统一 **4px 基准**：所有 padding/margin/行高取 4/8/12/16/20/24/28/32

#### 暗色主题
- 状态栏右侧 **🌙/☀ 按钮**（30×22 带细边框）一键切换
- 21 组颜色自动映射（`ThemeMap` 字典），运行时直接修改 `SolidColorBrush.Color`
- 偏好存入 `window.cfg`（`Theme=Dark`），启动自动恢复
- ComboBox 下拉列表、ContextMenu、ScrollViewer 均已暗色适配
- C# 常量 `LogReceivedColor` / `StatusDotIdle` 随主题同步切换

#### 接收区（RichTextBox 彩色日志）
- 系统消息（灰色）、发送回显（蓝色，可通过 CheckBox 关闭）、接收数据（深色/暗色下浅色）
- **时间戳格式可选**：ComboBox → 不显示 / `HH:mm:ss` / `HH:mm:ss:fff`（默认 fff）
- **智能滚屏锁定**：用户向上翻阅历史时暂停自动滚底，距底部 ≤8px 恢复自动滚
- **日志导出**："导出日志"按钮 → SaveFileDialog → 写入 .txt（UTF-8）
- 行缓冲：DataReceived 碎片拼完整行，100ms 空闲强制输出
- `MaxLogLines = 2000`，超出自动裁剪旧行
- **行号显示**：内嵌式右对齐 5 位（`    1  `），灰色 `TextMutedBrush`，CheckBox 开关，清空时计数归零
- **系统消息独立显示**（新增）：接收区上方 ListBox（2行高可滚动），`SecondaryHoverBgBrush`底 + 左侧3px边框，和纯设备数据区视觉区分；CheckBox 开关可切回旧行为（消息回流 RichTextBox）；切换时淡入淡出动画（200ms）

#### 发送区
- Enter = 发送（**PreviewKeyDown** 拦截），Shift+Enter = 换行
- **Placeholder 水印**："输入要发送的内容…"，有内容自动隐藏
- **发送历史下拉**：`▾` 按钮 → ContextMenu，最近 20 条（去重），空时显示"暂无历史记录"
- **定时发送**：CheckBox 开关 + TextBox 输入间隔(ms)，DispatcherTimer 自动重发
- MinHeight=68（约 3 行）→ MaxHeight=140（约 7 行）
- **HEX 实时格式化**：HEX 模式下自动过滤非法字符、每两字符插空格、转大写、光标位置保持
- **快捷发送按钮面板**：chip 风格按钮（`QuickSendChipStyle`），预置 AT 指令模板，左键立即发送/右键编辑删除，"＋"按钮快捷添加，"＋"按钮从发送区取内容，配置存 `%LocalAppData%\串口助手WPF\quick_sends.cfg`
- **换行符可选**（新增）：ComboBox：`\r\n`（默认）/ `\n` / `\r` / `无`，文本模式追加换行字符串，HEX 模式追加 HEX 字节，仅主发送区生效（快捷发送按钮已有内置换行）
- **发送后自动清空**（新增）：CheckBox 开关，发送后自动清空发送区

#### 状态栏
- 状态圆点 + 状态文字 + 串口信息
- **流量统计**：`TX: 1.2KB ↑  RX: 8.5KB ↓`，串口关闭显示 `--`，右键重置计数
- **流量持久化**（新增）：CheckBox `☑ 串口打开时不重置流量计数`，勾选后关闭/打开串口不清零，仅右键手动重置
- **自动重连**（新增）：CheckBox `☑ 自动重连`，USB 设备拔出后记住串口号，重新插入后自动恢复连接（500ms×10次轮询，最多5秒超时）

#### 动画 & 反馈
- 状态栏圆点：280ms **ColorAnimation** 平滑过渡（灰↔绿）
- 打开/关闭按钮：130ms **Opacity 脉冲**（1.0→0.55→1.0）
- **滑动开关**（新增）：200ms `Canvas.Left` 动画，白圆钮左↔右滑动，滑轨灰↔蓝变色
- **系统消息框淡入淡出**（新增）：200ms `Opacity` 动画，切换时平滑过渡

#### 字体
- UI：微软雅黑 9pt（窗口级继承）
- 标题：13pt SemiBold
- 数据区：等宽 `Sarasa Mono SC, Consolas, Courier New` 12pt
- **启动时字体检测**：`Sarasa Mono SC` 未安装 → 状态栏琥珀色提示 + 下载链接
- 更纱黑体 v1.0.39 已安装至系统（10 个 SC 字重）

#### 图标
- exe 文件图标：`TerminalSerial.ico`（.csproj `ApplicationIcon` 已配置）

---

## 设计系统

### 亮色色板（XAML 资源 Key）

| 用途 | 色值 | Key |
|------|------|-----|
| 窗口背景 | `#F0F0F0` | `WindowBgBrush` |
| 卡片背景 | `#FFFFFF` | `CardBgBrush` |
| 卡片边框 | `#E0E0E0` | `CardBorderBrush` |
| 主文字 | `#2D2D2D` | `TextPrimaryBrush` |
| 次要文字 | `#555555` | `TextSecondaryBrush` |
| 弱化文字 | `#999999` | `TextMutedBrush` |
| 主色（蓝） | `#0078D4` | `PrimaryBrush` |
| 主色悬停 | `#106EBE` | `PrimaryHoverBrush` |
| 主色按下 | `#005A9E` | `PrimaryPressedBrush` |
| 次要色 | `#888888` | `SecondaryBrush` |
| 次要色悬停 | `#666666` | `SecondaryHoverBrush` |
| 成功/连接（绿） | `#10B981` | `SuccessBrush` |
| 成功悬停 | `#059669` | `SuccessHoverBrush` |
| 状态栏底 | `#E6E6E6` | `StatusBarBgBrush` |
| 分隔线 | `#EEEEEE` | `SeparatorBrush` |
| 输入框边框 | `#D0D0D0` | `InputBorderBrush` |

### 补充色值（新增）

| 用途 | 色值 | Key |
|------|------|-----|
| 弱提示文字 | `#BBBBBB` | `TextSubtleBrush` |
| 微提示/禁用文字 | `#CCCCCC` | `TextFaintBrush` |
| 次要按钮悬停底 | `#F5F5F5` | `SecondaryHoverBgBrush` |
| 按钮禁用底 | `#E8E8E8` | `ButtonDisabledBgBrush` |
| 接收区底 | `#FAFAFA` | `ReceiveAreaBgBrush` |
| 状态圆点空闲 | `#CCCCCC` | `StatusDotIdleBrush` |

### 日志色

| 用途 | 色值 | Key |
|------|------|-----|
| 系统消息 | `#999999` | `LogSystemBrush` |
| 发送回显 | `#0078D4` | `LogSentBrush` |
| 接收数据 | `#2D2D2D` | `LogReceivedBrush` |

### 暗色色板对照（关键色）

| Key | 亮色 | 暗色 |
|-----|------|------|
| `WindowBgBrush` | `#F0F0F0` | `#1E1E1E` |
| `CardBgBrush` | `#FFFFFF` | `#2D2D2D` |
| `CardBorderBrush` | `#E0E0E0` | `#404040` |
| `TextPrimaryBrush` | `#2D2D2D` | `#E0E0E0` |
| `TextSecondaryBrush` | `#555555` | `#BBBBBB` |
| `StatusBarBgBrush` | `#E6E6E6` | `#252525` |
| `SeparatorBrush` | `#EEEEEE` | `#383838` |
| `InputBorderBrush` | `#D0D0D0` | `#505050` |
| `ReceiveAreaBgBrush` | `#FAFAFA` | `#222222` |
| `LogReceivedBrush` | `#2D2D2D` | `#E0E0E0` |
| `SecondaryHoverBgBrush` | `#F5F5F5` | `#383838` |
| `ButtonDisabledBgBrush` | `#E8E8E8` | `#333333` |
| `TextSubtleBrush` | `#BBBBBB` | `#666666` |
| `TextFaintBrush` | `#CCCCCC` | `#555555` |
| `StatusDotIdleBrush` | `#CCCCCC` | `#666666` |

完整 21 组映射见 `MainWindow.xaml.cs` 中的 `ThemeMap` 字典。部分色值（`PrimaryBrush`、`SuccessBrush` 等）在双主题下保持一致。

### 样式 Key（XAML 资源）

| Key | TargetType | 用途 |
|-----|-----------|------|
| `CardBorderStyle` | Border | 白底圆角卡片 + DropShadowEffect |
| `CardHeaderStyle` | TextBlock | 卡片标题（13pt SemiBold） |
| `FormLabelStyle` | TextBlock | 设置面板标签 |
| `PrimaryButtonStyle` | Button | 蓝底白字主按钮（含 Hover/Pressed/Disabled） |
| `SecondaryButtonStyle` | Button | 白底灰框次要按钮（含 Hover/Disabled） |
| `QuickSendChipStyle` | Button | 快捷发送 chip 标签按钮（24px高，圆角3，Hover 变深） |
| `SendTextBoxStyle` | TextBox | 发送区文本框（含 MinHeight/MaxHeight） |
| （隐式） | ComboBox | 含完整 ControlTemplate（下拉列表暗色适配） |
| （隐式） | CheckBox → ToggleSwitch | 40×22 圆角滑轨 + 18×18 白圆钮 + Canvas.Left 滑动动画 |
| （隐式） | ListBox | 只读日志样式（无选中高亮、透明底、无边距） |
| （隐式） | ContextMenu | 暗色适配 |
| （隐式） | MenuItem | 暗色适配 |
| （隐式） | ScrollViewer | 透明底色 |

---

## 新增功能详解

> **第一轮**（`0cb32fd` ~ `80de59a`）：流量统计、快捷发送、HEX格式化、行号显示。  
> **第二轮**（`7fbb35c` ~ `83fbdb2`）：换行符、发送后清空、自动重连、流量持久化、系统消息独立、滑动开关。

### 流量统计（第一轮）
- **位置**：状态栏，圆点和状态文字右侧
- **格式**：`TX: 1.2KB ↑  RX: 8.5KB ↓`（自动换算 B/KB/MB）
- **串口关闭时**：`TX: -- ↑  RX: -- ↓`（灰色）
- **右键**：点击流量文字 → 重置计数归零 + 日志提示
- **统计时机**：`SendData()` 累加 TX、`serialPort_DataReceived` 累加 RX

### 快捷发送按钮面板（第一轮）
- **位置**：发送区卡片内，标题下方、输入区上方（WrapPanel 自动换行）
- **预置指令**：`AT` / `AT+CWLAP` / `AT+CWMODE=1` / `AT+CIFSR` / `AT+RST`
- **交互**：左键 → 立即发送（调用 `SendRaw()`）；右键 → 编辑标签 / 删除
- **添加**："＋" 按钮 → 将当前输入区内容保存为快捷发送（弹出输入对话框命名）
- **持久化**：`%LocalAppData%\串口助手WPF\quick_sends.cfg`（tab 分隔格式）
- **架构**：提取 `SendRaw(string content)` 方法，快捷发送和 `SendData()` 共用

### HEX 输入实时格式化（第一轮）
- **触发**：发送模式为 HEX 时，`tbSend_TextChanged` 自动格式化
- **规则**：过滤非法字符（只保留 `[0-9A-Fa-f]`）→ 转大写 → 每两字符插空格
- **光标保持**：统计 hex 字符数映射到格式化后位置，防跳动
- **防重入**：`_isHexFormatting` 标志
- **模式切换**：切换到 HEX 时自动格式化现有内容

### 行号显示（第一轮）
- **方案**：内嵌式——每行日志前追加一个灰色 `Run`
- **格式**：右对齐 5 位（`$"{n,5}"`），如 `    1  `、` 1234  `
- **颜色**：`TextMutedBrush`（`#999999`），与内容色独立
- **换行**：一个 Paragraph = 一个行号，不因 wrap 拆行多编
- **开关**：接收设置卡片 `☑ 行号显示`，默认勾选
- **清空**：点击"清空接收区"时 `_lineCount` 归零
- **关闭后**：新日志不带行号，旧日志保留（行号已嵌入文本）

### ❌ 搜索功能（已回退）
- **原因**：WPF RichTextBox 的 `GetPositionAtOffset` 在 Paragraph 层级计数包含元素边界标记而非纯字符，导致光标位置错乱和崩溃
- **结论**：在该架构下难以根治，放弃

### 换行符可选（第二轮）
- **位置**：发送设置卡片 → ComboBox
- **选项**：`\r\n`（默认）、`\n`、`\r`、`无`
- **文本模式**：追加换行字符串到内容末尾（如 `AT` → `AT\r\n`）
- **HEX 模式**：追加 HEX 字节（如 `41 54` → `41 54 0D 0A`）
- **架构**：`SendRaw(content, appendLineEnding: true)` 仅主发送区触发，快捷发送按钮不受影响
- **辅助方法**：`GetLineEndingText()` / `GetLineEndingHex()` 提供映射

### 发送后自动清空发送区（第二轮）
- **位置**：发送设置卡片 → `☑ 发送后清空发送区` CheckBox
- **行为**：`SendData()` 调用 `SendRaw()` 之后执行 `tbSend.Clear()`
- 仅主发送区触发，快捷发送按钮不受影响

### 串口热插拔自动重连（第二轮）
- **检测**：`WM_DEVICECHANGE` → `DBT_DEVICEARRIVAL`（设备重新插入）
- **机制**：`lastPortName` 记住断开前端口 → `reconnectTimer` 每 500ms 轮询 → 最多 10 次（5 秒）超时
- **日志**：重连成功/超时均有系统日志提示
- **开关**：`☑ 自动重连` CheckBox，默认关

### 流量计数持久化（第二轮）
- **行为**：`OpenSerialPort()` 检测 `chkPersistTraffic.IsChecked`，勾选时跳过 `txByteCount/rxByteCount = 0`
- **开关**：`☑ 串口打开时不重置流量计数` CheckBox，默认关

### 系统消息独立显示（第二轮）
- **位置**：接收区卡片内，RichTextBox 上方（DockPanel.Top）
- **控件**：ListBox（2行高 36px，MinHeight=18、MaxHeight=72，超出自动滚动）
- **配色**：`SecondaryHoverBgBrush`底 + 左侧3px `InputBorderBrush`边框，和数据区视觉区分
- **上限**：最多 50 条，超出删除旧条目；自动滚到底
- **开关**：接收设置 `☑ 系统消息独立显示` CheckBox，默认勾选
- **取消后**：`LogSystem` 回流 RichTextBox → 恢复旧行为
- **动画**：勾选/取消时 200ms Opacity 淡入淡出
- **导出**：导出日志不含系统消息（只导 RichTextBox）

### CheckBox → ToggleSwitch 滑动开关（第二轮）
- **模板**：Canvas + 40×22 圆角滑轨（`InputBorderBrush`/`PrimaryBrush`）+ 18×18 白圆钮
- **动画**：`AnimateToggleSwitch()` → `Canvas.Left` 2↔20 滑动（200ms）
- **触发**：`IsChecked=True` Trigger 同步圆钮位置（`Canvas.Left=20`），解决初始位置错误
- **覆盖**：隐式 CheckBox Style，所有 7 个 CheckBox 自动获得滑动开关外观
- **暗色**：滑轨/圆钮色值均来自 `{StaticResource}`，亮暗自动适配

---

## 编译 & 运行

### 编译命令
```powershell
# 在 串口助手-WPF/ 目录下执行：
& "D:\Program Files\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe" `
  "串口助手-WPF.csproj" -t:Build -p:Configuration=Release
```
> 注意：参数前缀用 `-` 而非 `/`（某些 Shell 会把 `/` 解析为路径分隔符导致失败）。

### 编译后
将 `bin\Release\串口助手 WPF.exe` 复制到 `串口助手-WPF/` 根目录覆盖旧版。

### 运行
双击 `串口助手-WPF/串口助手 WPF.exe`。

---

## 已知注意事项

1. **等宽字体**：首选 `Sarasa Mono SC`（更纱黑体 v1.0.39 已安装），回退 `Consolas` → `Courier New`
2. **HEX 模式发送回显**：超过 80 字符截断预览
3. **日志行数上限**：`MaxLogLines = 2000`，超出自动裁剪
4. **行缓冲定时器**：100ms 空闲后强制输出残留文本
5. **窗口设置存储**：`%LocalAppData%\串口助手WPF\window.cfg`（13 行 key=value，每次关闭覆盖写不增长）  
   **加载分两阶段**：  
   - `LoadWindowSettings()` —— 构造函数早期执行，恢复 Left/Top/Width/Height/State/Theme（此时 ComboBox 尚未填充）  
   - `LoadPreferences()` —— `InitComboBoxItems()` + `SetDefaultValues()` 之后执行，恢复 ComboBox 选择 + CheckBox 状态（换行符、消息回显、行号显示、系统消息独立、自动重连、发送后清空、流量持久化）
6. **快捷发送存储**：`%LocalAppData%\串口助手WPF\quick_sends.cfg`
7. **暗色主题**：ComboBox/ContextMenu/ScrollViewer/ListBox 已有完整暗色适配，若有新增控件需确认
8. **ComboBox 模板**：已覆盖完整 ControlTemplate（ToggleButton + Popup），暗色下下拉列表底色为 `CardBgBrush`
9. **Enter=发送**：使用 `PreviewKeyDown` 事件（隧道事件），不可改回 `KeyDown`（冒泡，TextBox 内置处理会先执行）
10. **C# 常量同步**：`LogReceivedColor` 和 `StatusDotIdle` 在 `ApplyTheme()` 中随主题切换；新增日志颜色需同步加入 `ThemeMap`
11. **行号内嵌**：行号是文本的一部分，日志导出时会包含行号，复制选中文本也会带行号（此为设计取舍）
12. **RichTextBox 搜索**：曾尝试实现但 WPF RichTextBox 做高亮搜索有架构问题（`GetPositionAtOffset` 偏移计数），不建议重试
13. **系统消息框**：`lbSystemLog` 上限 50 条，用 `SecondaryHoverBgBrush`（`ThemeMap` 已有映射，亮暗自动适配）
14. **滑动开关**：隐式 CheckBox Style 已被 ToggleSwitch 模板覆盖，所有新加 CheckBox 自动获得滑动外观；`Knob` 和 `SwitchTrack` 是模板命名部件，`AnimateToggleSwitch()` 依赖这两个名字

---

## 待做 / 可优化方向

- [x] **串口热插拔自动重连** ✅（`ee00923` 及之前）
- [x] **发送后自动清空发送区** ✅
- [x] **发送区换行符可选** ✅
- [x] **流量统计偏好持久化** ✅
- [ ] **接收区搜索/过滤**：需换方案（如改用 AvalonEdit 或纯 TextBox），RichTextBox 方案已验证不可行
- [ ] **暗色主题调优**：用户对当前暗色页面不满意（"不太喜欢"），需重新审视暗色色板搭配，可能调整对比度、卡片层次、或引入更多暗色变体
- [ ] **主题切换按钮重新设计**：当前位置（状态栏右下角）太小（30×22）、太偏、全屏时容易误触 Windows 任务栏右下角图标；需换位置 + 放大 + 更醒目

> 以上 4 项已在 `7fbb35c` 中一次性实现。后续继续补充的系统消息独立显示（`ee00923`）、滑动开关（`4e506aa`）、偏好补全（`83fbdb2`）均为此基础上的增量。

---

## 重构计划（暂不执行，供未来参考）

> **状态**：MainWindow.xaml.cs 当前约 1700 行，尚在可控范围。以下拆分方案待文件增至难以维护时再执行。

### 建议拆分

| 新文件 | 移入内容 | 预估行数 |
|--------|---------|---------|
| `Services/ThemeService.cs` | `ApplyTheme()` / `ThemeMap` 字典 / `isDarkTheme` / `btnThemeToggle_Click` | ~80 |
| `Services/LogService.cs` | `LogSystem` / `LogSent` / `LogReceived` / `AppendColoredLine` / `receiveLineBuffer` / `flushTimer` / `_lineCount` / `_showLineNumbers` | ~120 |
| `Services/QuickSendService.cs` | `LoadQuickSends` / `SaveQuickSends` / `RefreshQuickSendButtons` / `ShowInputDialog` / `quickSends` 字典 | ~200 |
| `Services/WindowSettingsService.cs` | `LoadWindowSettings` / `SaveWindowSettings` / `LoadPreferences` / `SettingsFilePath` | ~100 |
| `Services/AnimationService.cs` | `AnimateBrushColor` / `PulseElement` / `AnimateToggleSwitch` | ~40 |

### 注意事项
- 每个 Service 需持有主窗口引用（或通过事件/回调通信）
- **不能动**的 4 个数据转换方法留在 MainWindow.xaml.cs
- 拆分后必须 0 error 编译通过
- 一次只拆一个 Service，拆一个测一个

---

## 行号独立方案讨论（暂未实现）

> **现状**：行号通过 `Run` 内嵌在 RichTextBox 的 Paragraph 中。**优点**：实现简单可靠；**缺点**：导出/复制文本带行号，且行号在文本流内无法独立控制。

### 备选方案 A：左侧独立 ListView/ItemsControl 同步滚动
- 行号和内容分两个控件，左边行号、右边数据，共享 ScrollViewer 同步
- **优点**：行号不污染数据，导出/复制纯净；可独立设置样式
- **缺点**：同步滚动实现复杂（需 hook ScrollChanged 事件双向同步）；增加布局复杂度；占用横向空间

### 备选方案 B：保持内嵌 + 导出时过滤
- 行号仍内嵌，但导出日志时用正则去掉行号前缀
- **优点**：改动极小，只在 `btnExportLog_Click` 加几行过滤
- **缺点**：复制粘贴仍带行号；过滤可能误删以数字开头的接收数据

### 备选方案 C：AvalonEdit 替换 RichTextBox
- 用 AvalonEdit（第三方 WPF 编辑器控件）替换 RichTextBox，其原生支持行号边栏
- **优点**：行号、语法高亮、搜索等开箱即用
- **缺点**：引入外部依赖（NuGet 包）；需重写大段日志逻辑；增加了 .exe 体积

### 当前决定
维持方案 B（内嵌）不动。若未来对导出/复制纯净度要求提高，优先尝试方案 B（导出时过滤）。

---

## 暗色主题调优（暂未实现）

> **用户反馈**：当前暗色页面不太满意，以后改。

### 现状
- 21 色完整映射，亮暗一键切换
- 色板参照主流暗色 IDE（`#1E1E1E` 窗底 / `#2D2D2D` 卡片 / `#E0E0E0` 主文字）

### 可能的方向

**A. 微调色板**
- 当前暗色卡片底 `#2D2D2D` 和窗底 `#1E1E1E` 对比度偏低，卡片边界不够清晰
- 建议：卡片底提到 `#333333`，或加强 `CardBorderBrush`（`#404040` → `#505050`）
- 接收区底 `#222222` 和卡片底 `#2D2D2D` 在暗色下几乎分辨不出

**B. 双暗色方案**
- 当前只有一套暗色色板；可加一套"深黑"（OLED 纯黑系 `#000000` 底）和"柔和暗"（蓝灰调 `#1A1D23`）
- 用 ComboBox 替换现在的 🌙/☀ 按钮，选项：亮色 / 暗色 / 深黑

**C. 参考成熟产品**
- VS Code Dark+、JetBrains Darcula、iTerm2 暗色——这些配色经过大量用户验证
- 直接"借"它们的色板比例，映射到当前 21 色体系

---

## 主题切换按钮重设计（暂未实现）

> **用户反馈**：右下角太小（30×22）、太偏、全屏时容易误触 Windows 任务栏右下角图标。

### 现状
- 位置：状态栏 `HorizontalAlignment="Right"`，窗口最右下角
- 尺寸：`Width="30" Height="22"`，内容 🌙/☀
- 问题：全屏最大化时状态栏紧贴 Windows 任务栏，按钮恰好叠在系统托盘区域上方，鼠标点击容易穿透/误触

### 方案对比

| 方案 | 位置 | 优点 | 缺点 |
|------|------|------|------|
| **A. 左移** | 状态栏左侧（`tbTraffic` 旁边） | 改动最小（1 行 XAML）；远离任务栏 | 仍在底部，不够醒目 |
| **B. 设置面板顶部** | 右侧设置面板最上方，独立卡片或大按钮 | 远离任务栏；作为"设置"的一部分更合理；可做大 | 占用面板空间 |
| **C. 标题栏内嵌** | 窗口标题栏右侧（自定义 Chrome） | 不占工作区；始终可见 | 需 `WindowChrome` 重写标题栏，改动大 |
| **D. 扩大 + 浮动** | 保持右下但加大到 44×32 + 加粗边框 | 改动最小 | 仍可能误触；没解决根本问题 |

### AI 推荐：方案 B（设置面板顶部）

理由：
1. 设置面板是目前最"安全"的区域——左边收发区、底部状态栏都可能和系统 UI 冲突，只有右侧面板永远在窗口内部
2. 主题切换本质是"设置"而非"状态"，放设置面板语义更对
3. 可以做成醒目的大按钮——比如 48×28、带图标+文字（"🌙 暗色模式" / "☀ 亮色模式"）、放在设置面板最顶部
4. 状态栏的 ☀/🌙 按钮可移除或保留为快速指示器

### 具体建议

```
┌─ 设置面板 ──────────────────────┐
│ ┌─────────────────────────────┐ │
│ │  🌙  暗色模式               │ │  ← 48×28 大按钮，PrimaryButtonStyle
│ └─────────────────────────────┘ │     整行宽，点击切换
│                                 │
│ ┌─ 串口配置 ──────────────────┐ │
│ │ 串口号  [COM3       ▾]     │ │
│ │ ...                         │ │
```

- 按钮文字随主题变化："🌙 暗色模式"（当前亮色时）/ "☀ 亮色模式"（当前暗色时）
- 可选：在设置面板底部 / 状态栏左侧保留一个小指示器（只读，不点击）

---

## 给 AI 的引导

### ⚠️ 前端设计技能

用户安装了多个前端设计 skills（**design-taste-frontend**、**ui-ux-pro-max**、**minimalist-ui**、**industrial-brutalist-ui**、**antfu**、**vue 生态**等）。虽然它们大多是 Web 方向，但其设计思维同样适用于 WPF XAML：

- **间距与节奏**：4px 基准系统（已在用）
- **色彩语义化**：命名色板而非硬编码（已在用）
- **组件化思维**：可复用 Style / ControlTemplate（已在用）
- **动效克制**：动画有明确目的而非炫技（已在用）
- **可访问性**：对比度、字号层级

在改动 UI 时可以参考这些 skills 的设计理念。

### 工作流程（每次必做）
1. **先读** `串口助手-WPF/当前状态.md`（本文件）
2. **再读** `MainWindow.xaml` 和 `MainWindow.xaml.cs`
3. **改动前先 git commit** 存档
4. **改动范围**：见上方 ⚠️ 硬性约束
5. **改动后必须编译**：0 error 才算过，编译命令见上方
6. **编译成功后**：复制 exe 到项目根目录
7. **再 git commit** 存档

### 什么能改
- `MainWindow.xaml`：布局、样式、颜色、字体、动画、控件增删
- `MainWindow.xaml.cs` 中的日志系统：`LogSystem` / `LogSent` / `LogReceived` / `AppendColoredLine` / `receiveLineBuffer` / `flushTimer`
- 发送区行为、键盘快捷键、定时发送、发送历史
- 新增控件及事件处理
- 暗色主题系统：`ApplyTheme()` / `ThemeMap` / `isDarkTheme`
- 动画系统：`AnimateBrushColor()` / `PulseElement()`
- 窗口设置系统：`LoadWindowSettings()` / `SaveWindowSettings()`
- 快捷发送系统：`LoadQuickSends()` / `SaveQuickSends()` / `RefreshQuickSendButtons()` / `SendRaw()`
- HEX 格式化：`AutoFormatHexInput()` / `_isHexFormatting`
- 行号系统：`_lineCount` / `_showLineNumbers` / `chkShowLineNumbers_Changed`
- 流量统计：`txByteCount` / `rxByteCount` / `UpdateTrafficDisplay()` / `FormatBytes()`
- 换行符系统：`GetLineEndingText()` / `GetLineEndingHex()` / `cbLineEnding`
- 自动重连系统：`lastPortName` / `reconnectTimer` / `_reconnectAttempts` / WndProc 中的 `DBT_DEVICEARRIVAL`
- 系统消息独立显示：`lbSystemLog` / `chkSeparateSystemLog_Changed` / `LogSystem` 中的路由逻辑
- 滑动开关：`AnimateToggleSwitch()` / CheckBox 隐式模板中的 `Knob`、`SwitchTrack`、`Canvas.Left` 动画

### 什么绝对不能改
- 4 个数据转换方法：`BytesToHex` / `HexToBytes` / `BytesToText` / `TextToBytes`
- `OpenSerialPort` / `CloseSerialPort` 中的串口操作逻辑（改动仅限于按钮颜色、状态栏文字、日志输出）
- `serialPort_DataReceived` 中的 `serialPort.Read` 部分
- `byteBuffer` 的多字节拼接逻辑
- USB 热插拔检测 `WndProc`

### 本轮关键决策速查（供新 AI 快速理解"为什么"）

| 决策 | 原因 |
|------|------|
| `SendRaw(content, appendLineEnding)` 而非直接在 SendRaw 内追加 | 快捷发送按钮已有内置换行，不能重复追加 |
| 系统消息用 DockPanel+ListBox 而非第二个 RichTextBox | 轻量、样式可控、不用再搞一套 FlowDocument |
| CheckBox 模板用 Canvas+Canvas.Left 而非 TranslateTransform | TranslateTransform 是 Freezable，ControlTemplate Trigger 找不到它 |
| `LoadPreferences()` 独立于 `LoadWindowSettings()` | ComboBox 必须先填充 Items 才能设置 SelectedIndex |
| `AnimateToggleSwitch()` 在每个 CheckBox 事件处理器中调用 | WPF 的 EventTrigger 不支持 Checked/Unchecked，只能 code-behind |
| `window.cfg` 用 `File.WriteAllLines` 覆盖写 | 文件不增长，13 行永远 350 字节 |
| C# 语言版本锁定 7.3 | .NET Framework 4.8 的默认值，switch 表达式等新语法不能用 |
